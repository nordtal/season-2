<!--
  deploy/dev deploy

  Rebuilds all four plugins, swaps the jars and restarts those four containers. The four
  single-server variants beside this one are the fast loop; this is the one after a change in
  :common, which all four carry.

  A JAR SWAP AND NOTHING ELSE: a changed deploy/dev.env does not reach a restarted container.
  Use `dev up` after editing that file.

  Why this runs the way it does - the interpreter, the terminal, the working directory - is in
  .run/README.md, once, rather than in each of these thirty files.
-->
<component name="ProjectRunConfigurationManager">
  <configuration default="false" name="dev deploy (all four)" type="ShConfigurationType" folderName="dev: build and deploy">
    <option name="SCRIPT_TEXT" value="" />
    <option name="INDEPENDENT_SCRIPT_PATH" value="true" />
    <option name="SCRIPT_PATH" value="$PROJECT_DIR$/deploy/dev" />
    <option name="SCRIPT_OPTIONS" value="deploy" />
    <option name="INDEPENDENT_SCRIPT_WORKING_DIRECTORY" value="true" />
    <option name="SCRIPT_WORKING_DIRECTORY" value="$PROJECT_DIR$" />
    <option name="INDEPENDENT_INTERPRETER_PATH" value="true" />
    <option name="INTERPRETER_PATH" value="/usr/bin/env" />
    <option name="INTERPRETER_OPTIONS" value="bash" />
    <option name="EXECUTE_IN_TERMINAL" value="true" />
    <option name="EXECUTE_SCRIPT_FILE" value="true" />
    <envs />
    <method v="2" />
  </configuration>
</component>
