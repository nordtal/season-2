# State of play

Where season 2 actually stands, what can be built without another planning session, and what
cannot.

**Re-derived from the code on 2026-09-01, and again the same day after `limbo` and the pack station
were built.** An earlier version of this document was written on
2026-08-31 *before* that evening's implementation work and then committed *after* it (`6fcede9`,
23:33, against `e1dd4ba`, 21:17): it described `hunger-games` as a 22-line scaffold, `common`'s
`SeasonPhase` as holding three wrong values, `Glyphs` as season 1's inventory and the whole of §2 as
unbuilt, all of which had stopped being true hours earlier. That is the failure mode this document
warns about in its own last line, so the numbers below were read out of the repository again rather
than edited.

This is a survey of the **code**, not of the plan. Where a document says something the code does not
support, that is recorded as a finding rather than smoothed over. The plan lives in the other
documents and is not repeated here.

It is expected to go stale. Re-derive it rather than trust it once a module has been implemented.

## 1. Where we stand

### The short version

| module | main Java | tests | what actually runs |
|---|---|---|---|
| `smp` | 94 files, 13274 lines | 212 | Everything docs/smp.md describes. Rehearsed on the local stack 2026-09-05/06 with two real clients, which found findings 96 to 101; what is still unseen is the *world* - Nordtal, the arena, the spawn geometry - because it is in no repository |
| `common` | 66 files, 7244 lines | 351 | Access API, messages, locales, phase, glyphs, the limbo protocol, readiness, the notification listener, **the command request table**, V1-V11 |
| `network-control` | 40 files, 6177 lines | 195 | Login gate, phase, play time, routing, the pack station, **the Velocity command adapter** |
| `discord-bot` | 51 files, 7846 lines | 145 | Access end to end, the admin mirror, the language list, hunger games registration, **every declared command as a slash command** |
| `updater` | 38 files, 5179 lines | 151 | Resolve, report, apply, serve - and the schema every other process waits on |
| `commands` | 46 files, 4131 lines | 180 | **Every admin command in the network**, its declaration, its decisions, and both ends of the request row |
| `hunger-games` | 44 files, 4944 lines | 72 | The start event, essentially in full |
| `limbo` | 13 files, 1640 lines | 11 | The waiting room, in full |
| `paper-common` | 5 files, 1221 lines | 5 | The operator adapter, the admin watcher, the Paper sender adapter and **the Paper command adapter** |
| `resource-pack` | — | — | Four fonts, every code point allocated and drawn |

1392 tests, none skipped, all green with a Docker daemon present (`./gradlew build`, 2026-09-09,
on `release/0.7.1` after `/update` was folded onto every surface and the countdown rebuilt, PR #10),
across **nine** modules — `resource-pack` has no test source set of its own, and `paper-common`
gained one on 2026-09-05 with the command adapter.

**This table was three days stale until 2026-09-04, and that is worth writing down rather than
quietly fixing.** It read 887 across seven modules with `:updater`, `:commands` and `:paper-common`
missing outright and every remaining row low - `common` said 98 where the reports said 289. A
document whose first section warns that "a document describes the state on the day somebody wrote
it" had its own headline number describing 2026-09-01. The counts were 730 on 2026-09-02 and 854 on
2026-09-03.

Every number in this table is read out of the JUnit XML and the file tree, never counted by eye:
this line once read 435 while the rows above it added to 438, and `season-2/CLAUDE.md` claimed 13
tests each for `AuraPayoutTest` and `TrackValidationTest` and 7 for `DeathPenaltyTest` where the
reports say 12, 12 and 8. Both were corrected on 2026-09-01 by re-reading the reports.

**No module is a scaffold any more, and no half of one is missing either.** Every feature
docs/smp.md describes exists in code as of 2026-09-01. What is left is not building but *watching*:
none of `smp`'s 188 tests touches a world, a packet or a player, because none of them can, and this
document has said since it was written that "it compiles" proves nothing here. The watching started
on 2026-09-05 on the local stack, and the six findings it produced in two days (96 to 101, one of
them a table nothing ever wrote a row to) are the argument for the sentence.

### `common`

Everything the plan gives this module exists:

- `access/` — `AccessDirectory` and the login query, unchanged and well covered.
- `message/` — `Messages`, `Locales`, and **`PlayerLocales`**, the join-time component
  [i18n.md](i18n.md) asks for: loaded once at join, held for the session, never re-queried from a
  render path, English for a player nobody called `join` for.
- `phase/` — `PhaseDirectory`, `PhaseDao`, `JdbiPhaseDirectory`, `PhaseChange`. The switch and its
  audit entry are one method, which is what [season-phases.md](season-phases.md#who-may-switch-it)
  demands of two writers.
- `network/` — **new 2026-09-03.** `SnapshotDirectory` and the one query behind every count the
  network shows in public. It moved here out of `network-control` when the Discord bot gained a
  status channel that says the same things; it reads `hg_*` and `smp_*`, which two other modules
  own, and names the migrations it depends on for that reason.
- `SeasonPhase` — the four agreed values, with `fromDatabase` falling back to `MAINTENANCE`.
- `Glyphs` — season 2's inventory across all four fonts: the donor star, thirteen prestige crests,
  the board frame pieces, the bossbar icons and arrows.
- `limbo/` — `LimboProtocol` and `WaitReason`, the `nordtal:limbo` wire format (2026-09-01). It is
  here rather than in either module that speaks it because both ends are in this repository and a
  format written twice drifts; a plugin message that does not parse looks exactly like one that was
  never sent, so the failure would be a player sitting in the waiting room forever with nothing in
  any log.
- `db/migration/` — V1 access, V2 bot state, V3 bot setting, V4 phase/admin/playtime, V5 hunger
  games, **V6 the SMP** (2026-09-01), V7 update requests, **V8 the `PRE_LAUNCH` phase and
  `season_phase.launch`**, **V9 `season_phase.smp_start`** — the second date, the one paid access is
  anchored to (both 2026-09-03).

### `network-control`

- `gate/` — `LoginGate` decides per phase; `MisconfiguredGate` is the deny-all `LoginEvent` handler
  that makes a broken config fail closed; `FallbackCache` and `ExpiryWatch` unchanged.
- `phase/` — `PhaseWatch` (30-second poll, last-known-phase fallback), `PhaseListener` and
  `PostgresPhaseNotifications` (`LISTEN nordtal_phase` on a dedicated connection outside the pool),
  `PhaseCommand` (the emergency `/phase`, authorised by the admin flag).
- `playtime/` — `PlaytimeWriter` / `PlaytimeStore` / `PlaytimeDao` into `player_playtime`, flushed
  on disconnect, on shutdown and every 300 seconds.
- `routing/` — `PhaseServers`, `PhaseRouting`, `PlayerRouter`, `RouteDecision`. A phase change
  re-routes everyone; a `MAINTENANCE` login lands in `limbo`; a switch to `SMP` disconnects a player
  without access, and `decideInitial` puts every login in `limbo` first.
- `ping/` — **new 2026-09-03.** `NetworkPing` answers `ProxyPingEvent` with the MOTD for the current
  phase and the one advertised limit, both from `network.yml`; `Placeholders` substitutes the
  braces; `SnapshotStore` keeps the numbers behind them fresh on a ten-second timer, because a ping
  is unauthenticated and must never reach the database. **The query itself moved to
  `:common/network` on 2026-09-03** (`SnapshotDirectory`), because the Discord bot renders the same
  counts into a channel name and two queries computing "the same" numbers is how two public
  surfaces start disagreeing. What is left here is the cache and the failure rule.
- `launch/` — `LaunchCountdown`, shared by the server browser and the three `PRE_LAUNCH` disconnect
  screens, so the network counts down to one instant in one wording.
- `pack/` — **built 2026-09-01, and it was the module's last gap.** `PackStation` (the forced
  offer, the `nordtal:limbo` channel, the release), `PackOffer` (one `ResourcePackInfo` per language,
  id derived from the pack's own hash), `PackMessages` and `LimboHold` — the last of which is the
  pure rule deciding what a waiting player is told, and the only part of the station a test can
  reach.

`PlayerRouter` now sets `limbo` as the initial server in **every** phase, `PhaseRouting#decideInitial`
is that rule, and a proxy with no waiting room refuses the login rather than falling through to
`velocity.toml`'s `try` list. `pack.yml` is new and is the first config file in this repository whose
values change on a release cadence.

**What this module still owes is a rehearsal, not code.** It owns three open verifications and none
of them can be answered here — see [the unverified assumptions](#the-unverified-assumptions) below.

### `discord-bot`

- `access/` — the sales, matching, linking and admin surface. `RedemptionLimit` (2026-09-03) is the
  five-wrong-codes-an-hour cap that makes a four-character link code safe; `SeasonStart` is the
  warning on every grant written while the season has no start date.
- `discord/PhaseCommand` — `/phase set`, the normal path for switching the phase.
- `discord/AdminFlagDao` — the admin-role mirror into `discord_user.admin`.
- `config/Languages` + `DefaultLanguages` — the `languages` list; a third language is a config edit,
  proven by a test that drives an `fr` entry no source file mentions.
- `hungergames/` — `RegisterFlow`, `Teams`, `RegisterMessages`, the invitation accept/decline, the
  managed message per language channel.
- `status/` — **new 2026-09-03.** `StatusName` renders the sidebar channel name for a phase and a
  snapshot, and is pure; `StatusChannels` is the minute timer that renames one channel per language
  and everything that keeps it inside Discord's two-per-ten-minutes budget.
- The bunq environment is a config key rather than a hardcoded `PRODUCTION`.

What it does not have is an SMP surface — announcements, admin commands — and that is correct: the
module it would talk to does not exist.

### `hunger-games`

Built on 2026-08-31 and covering nearly the whole of [hunger-games.md](hunger-games.md):
`border/` (`BorderController`, `BorderMath`), `loot/` (`LootRefill` with four configured tiers),
`hud/` (`HudRenderer`, `BossBarWidth`, `Bearing`), `lobby/` (`Lobby`, `LobbyMaps`), `body/`
(`PlayerBodies` — the disconnected body that stays and stays vulnerable), `color/` (`TeamColours`,
the generated palette and its nearest-named mapping), `game/` (`HungerGamesManager`, `SpawnTowers`,
`WinTracker`, `Tiebreak`, `Demotion`, `Ceremony`), `db/` and `command/HungerGamesCommand`
(`/hg start`, `confirm`, `ready`, `ready-status`, Brigadier only).

The loot pools, the quiet period and the passive shrink rate — three of the four "still open" config
points in that document — now exist as defaults in `HungerGamesSpec` and `DefaultRefillTiers`.

**Changed on 2026-09-01, from outside its own session:** the join-time language lookup moved off the
main thread and `database.yml` gained a `query-timeout-seconds`. Nothing about the game changed —
see finding 12 below for why a `limbo` session touched this module at all.

**Changed again on 2026-09-01, second pass** — findings 15 and 16, plus a message-key sweep:

- `/hg start` no longer runs its database work on the main thread, and the roster read moved out of
  the release tick.
- The **lobby countdown speaks**. It said nothing at all: players were teleported onto a tower,
  frozen and released a minute later with no text on screen. `Countdown` is the (tested) rule for
  when it speaks; `hg.start.countdown` had been written and translated the whole time.
- A **demoted duo is told it was demoted.** `Participant#demotedToSolo` had been computed and
  tested since the module was built and read by nothing.
- The **ceremony can tell the four endings apart**, so a win decided on the kill count is announced
  as one.
- Nine unreachable message keys: three wired up as above, three deleted (a duplicate of the winner
  line, a console guard Brigadier makes unreachable, a database-error text with no catch block near
  it), and two — `hg.lobby.map-missing` and `hg.loot.point-lost` — annotated in both language files
  as deliberately unsent, with the reason. `MessageBundlesTest` now holds the two files symmetric.

What is missing is not code:

- The **world folder** — the hand-built map, lobby box, spawn towers and loot points. The plugin
  disables itself rather than run without it.
- The **aerial and rules images**. `lobby/map-en.png` and `map-de.png` ship as dummy placeholders,
  and `LobbyMaps` is deliberately tolerant of them being wrong.
- The **winner's head start**, which is now the SMP's job and not this module's — see below.

### `resource-pack`

Season 1's leftovers are gone: the four role tags are deleted from `default.json` and from the
textures, and `en_us.json` is season 2's. Three fonts are documented and mirrored in `Glyphs`:
`minecraft:default`, `nordtal:bossbar` and `nordtal:board` (added 2026-08-31, so the board frame
does not have to share `default.json`'s metrics).

**Every allocated code point now has a PNG**, generated by `tools/generate_dummy_textures.py` on
2026-08-31 and marked in the pack's README as either `generated — final candidate` or
`generated — placeholder`. That is what makes the HUD and the boards exercisable on a running server
before the real design work happens. The balloon has a scaffolded item model with flat-colour
textures standing in for the 3D model.

### `limbo`

Built 2026-09-01, and it is small on purpose — its entire interface is one title.

- `world/` — `VoidChunkGenerator` (every `shouldGenerate*` false) and `WaitingWorld`, which creates
  the empty world, pins its gamerules and owns the one location anybody stands in. Players are put
  there by `AsyncPlayerSpawnLocationEvent`, *before* they are spawned, so the server's own
  `level-name` world is never seen for a frame.
- `waiting/WaitingRoom` — adventure mode, flying, invulnerable, blind, and the title. The title is
  re-sent on a timer because a Minecraft title expires, and an expired one here is a completely
  black screen with nothing on it, which looks exactly like a hung client. A *change* of reason
  fades in; a refresh does not.
- `net/LimboChannel` — `READY` out, `WAIT` in.
- `listener/PresenceListener` — no join message, no chat, no damage, no hunger, no interaction, and
  every player hidden from every other in both directions.
- `config/`, `db/` — `config.yml`, `database.yml`, and a HikariCP pool of three connections for the
  one query this module ever makes.

The `GameRule` constants were all renamed in 1.21.11 and the old names are `@Deprecated(forRemoval)`;
this module uses `GameRules` throughout, which is worth knowing before the SMP writes its own worlds.
`World#setKeepSpawnInMemory` is a no-op since 1.21.9 and is not called.

### `smp`

**Two thirds built, 2026-09-01.** The server-free core came first; the worlds followed the same
day, after two throwaway drills on a real Paper 26.2 server settled the two questions the design
was resting on (see [the unverified assumptions](#the-unverified-assumptions)).

- `config/` - `SmpSpec` (`config.yml`), `DatabaseSpec`, and **`MilestonesSpec` + `DefaultTrack`**,
  which is the track of docs/smp.md#the-track written out entry for entry. `DefaultSmp` carries the
  six config points that used to be listed as open: the advancement awards, the death causes, both
  duel loadouts, the winner's head start, the wheel's pool and the spawn regions.
- `milestone/` - `MilestoneTrack`, `Milestone`, `Objective`, the three enums, `StoredProgress`,
  `TrackShape` (is this file a track at all), `TrackValidation` (may it replace the running one) and
  `ObjectiveProgress`.
- `aura/` - `AuraPayout` (the 30/70 split, the 2 % threshold, the minimum share and the admin
  scaling), `AuraReason`, `DeathPenalty`.
- `prestige/` - `Prestige`, thirteen tiers derived from `player_playtime.seconds` and never stored.

Then, the same day, the world half's first block:

- `world/` - `Worlds` (find or create the four, hold their borders, animate Nordtal's expansion),
  `WorldRole`, and `Datapacks`, which verifies rather than installs - see below.
- `farm/` - `FarmWorldSwap` (unload, rename aside, rename in, reload, delete off-thread),
  `FarmWorldReset` (the clock, the warnings, the postponement), `DailySchedule` and `LandingSite`.
- `pregen/` - `PreGenerator`, a thin wrapper over `ChunkyAPI` from Bukkit's `ServicesManager`.
- `travel/` - `BalloonMenu` (the 2 x 2 layout, pure and tested as a table), `BalloonGui`,
  `BalloonListener` and `PortalGate`.
- `protect/` - `ProtectionListener` and `AdminFlags`; `region/` - `Box`, `Boxes`, `ConfigBoxes`.
- `state/SeasonState` - where the database's progress and the file's definition meet.
- `db/` - `SmpPool`, `SmpDao` and `JoinGate`, which refuses logins while PostgreSQL is unreachable
  and keeps the people already playing.

`build.gradle.kts` takes jcore, HikariCP, jdbi3-postgres and - both `compileOnly`, never shaded -
`com.github.nordtal:papermc-display-tags:2.0.0` and `org.popcraft:chunky-common:1.5.3` (from
CodeMC; the `chunky` coordinates on Maven Central are unrelated projects). `paper-plugin.yml`
declares both DisplayTags and Chunky with `load: BEFORE` and `required: true`.

**`SmpPlugin` refuses to start on three conditions**, each because the damage cannot be undone
afterwards: a required datapack that is not enabled, a missing Nordtal, and a balloon outside the
radius 10-21.5 band that makes the first milestone mean anything.

**What is not here** is the player surfaces - HUD, the per-player Text Display boards, nametags,
`/navigate`, POIs, prestige rendering - and the activities: duels, graves, the wheel, milestone
completion with its payout, and `/smp`.

### Where the documents and the code disagree

The nine findings of the previous version are resolved as follows, with everything found since
appended. **Findings 67 to 73 are new on 2026-09-05 and every one of them was found by writing a
second implementation next to the first, not by a test** - which is the argument for the fold, made
after the fact. **Findings 102 to 114 are new on 2026-09-06 and every one of them came from the
CodeRabbit review of PR #8**, which is worth its own sentence: they are the first block in this
table found by *reading* rather than by running, they include the only one with a security shape
(102, an admin-only command reachable by anybody) and the only other one that could have deleted a
world (113), and two of them - 102 and 106 - were sitting underneath a comment that said the
opposite. **Findings 9, 30, 60, 94 and 148 to 152 still stand** - 9 and 30 by decision, 60 because only a real ear can answer it, 94 because it is documented rather than fixed, and 148 to 152 because they were found on 2026-09-08 in the owner's review of every open item and are agent work in the workspace's `aufgaben.md`. 126 closed on 2026-09-07 when the owner chose the delay and it was built (`FarmWorldReset.STAGING_DELAY`), 147 on 2026-09-08 with the update redesign. Every other row is closed; 19 by its own row, 54 by the command table in `docs/README.md`, and **22 and 33 on 2026-09-06 by measuring instead of asking** - all four images are pushed by `release.yml` and all four packages answer an anonymous pull with HTTP 200. This sentence has now been wrong three times: twice by naming fewer open rows than the table underneath carried, and once by naming two that had quietly become true. Both directions are the same failure, and both were caught by somebody checking rather than reading. Keeping that sentence
accurate is itself a habit worth having — an earlier version read "only two still stand" while the
table underneath had one row marked as standing, which is the same class of drift the table exists
to record.

| # | what it was | now |
|---|---|---|
| 1 | The login gate is phase-blind | **closed** — `LoginGate` decides per phase, `MAINTENANCE` holds in `limbo` |
| 2 | The proxy accepts logins un-gated on a broken config | **closed** — `MisconfiguredGate` |
| 3 | Two files point at a deleted document | **closed** 2026-08-31 |
| 4 | `docs/README.md` overstates `network-control` | **closed** — four of the five things are built; only the pack station is not |
| 5 | `app.simplecloud.api:api` is a dependency nothing imports | **closed 2026-09-01** — the dependency, its two repositories and its catalog entry were removed. Routing resolves backends by `gate.yml` name through `ProxyServer.getServer(name)`, which is exactly the fallback that had been written down for it |
| 6 | `link-code-ttl-minutes` duplicated | **closed** — retired from `AccessSpec`, with a test asserting the key is dropped from a deployed file (it asserted the key was *refused* until jcore 3.1.0 on 2026-09-05) |
| 7 | The jar-size claim was wrong | **closed** — corrected in both documents |
| 8 | A whole font was undocumented | **closed** — the pack's README owns all three fonts |
| 9 | The bossbar font's positive space advances sit outside the private-use area | **stands.** `！` and friends are `FULLWIDTH EXCLAMATION MARK`, not private use, while the pack states its range as ``–``. Confined to `nordtal:bossbar` and `nordtal:board`, so nothing is broken; moving them would change the HUD code that composes the bar, which now exists |
| 13 | **New 2026-09-01:** jcore's config system had never been asked for two levels of nesting, and nothing said whether it could do it | **closed by building it.** It can - a `List<NestedSpec>` inside a `NestedSpec` round-trips - but **every nested interface needs its own `@ConfigSpec`**, and one without it fails as a Gson error about making `java.lang.reflect.Proxy#h` accessible, which names nothing useful. `MilestonesTest` is the standing proof; this is worth knowing before the next nested config is written |
| 12 | **New 2026-09-01:** `hunger-games` read a player's language with a blocking JDBC call from its `PlayerJoinEvent` handler, on the main thread, and its pool had no timeout of any kind | **closed the same day, in both modules.** `PlayerLocales#joinAsync` runs the lookup on Bukkit's async scheduler; `limbo` redraws its title when the value lands, `hunger-games` needs no redraw. Both `database.yml` files gained `query-timeout-seconds` (default 3), setting HikariCP's `connectionTimeout` and the driver's `socketTimeout`. Found while building `limbo`, where the same line would have frozen the server every login passes through |
| 11 | **New 2026-09-01:** the knowledge base named `objects.githubusercontent.com` as what a GitHub release asset redirects to | **closed by measurement.** It is `release-assets.githubusercontent.com`, one hop, with a signed URL that expires within the hour. Corrected in [resource-pack/README.md](../resource-pack/README.md#hosting) and in `PackSpec`'s own comment, which now says in capitals not to paste the resolved address into the config |
| 10 | **New 2026-09-01:** the knowledge base described three modules as unbuilt that were built hours earlier, and `season-2/CLAUDE.md` still opens with "`hunger-games`, `smp` and `limbo` are still scaffolds" | **closed by this pass** in both places. The cause is worth keeping: a documentation commit that lands *after* an implementation commit is not evidence that it describes it |
| 14 | **New 2026-09-01, second pass:** §3 of this document still listed six config defaults as open — duel loadouts, the advancement awards, the "embarrassing" death causes, the objectives' items, the wheel's pool, the winner's head start — while its own §2b, `docs/smp.md#still-open`, `DefaultSmp` and `DefaultTrack` all said they had been written the same day | **closed by reading the code.** All six are in `DefaultSmp`/`DefaultTrack`; §3 now says so. The cause is the one this document keeps rediscovering: a list that lives in a different file from the thing it describes goes stale silently, and a *closing* edit is as easy to miss as an opening one |
| 15 | **New 2026-09-01, second pass:** `/hg start` did every one of its database calls on the main thread. `HungerGamesCommand#runAsAdmin` checked the admin flag asynchronously and then handed the *action* back to `runTask`, so `dao.game`, `dao.roster`, `HungerGamesManager#start`'s second roster read and its one colour write per team all landed on the server thread — and `winTracker.reset(dao.activeMembersOf(…))` ran inside the `onReleased` callback, in the tick every participant is released | **closed the same day.** `runAsAdmin` runs the action on the async task it is already on, the messages hop to the main thread through a `tell` helper, and the roster read moved up into `HungerGamesPlugin#startGame`, which also closes a race the callback had. `HungerGamesManager#start`'s own closing `runTask` had been the standing evidence that it was written to be called from off-thread; nothing read it. This is **finding 12 again, in the same module**, three weeks of nothing between them |
| 16 | **New 2026-09-01, second pass:** `WinTracker.Outcome` carried a `tie` flag no caller ever read, and `Outcome::win` set it to `false` even for a win the tiebreaker produced. A game decided on the kill count was announced as an ordinary win, and `hg.win.tie-broken` and `hg.win.no-winner` — written, translated and carrying `{winnerKills}`/`{loserKills}`/`{kills}` — were unreachable | **closed the same day.** `Outcome` now has four constructors for the four endings and carries the two kill counts; `Ceremony` prints all four. "Everybody dead, no simultaneous pair" is `noWinner()` rather than a tie, because nothing was compared |
| 17 | **New 2026-09-01, third pass:** **Terralith and Dungeons and Taverns appeared in no document, no config and no container** — yet they are what the terrain of every world in this season *is*. Nordtal is pre-generated once to border 4000, freezing whichever pack version produced it, while the farm world is regenerated nightly | **closed the same day.** Both are pinned by sha512 (Terralith 2.6.4, D&T 5.3.2, `datapack` loader, verified against a real download), fetched by `deploy/minecraft/entrypoint.sh` before the server starts, and verified at enable by `smp`, which refuses to start when one is missing. The cause is the one worth keeping: a dependency that lives in the operator's head and not in a file is invisible to everyone who was not there |
| 18 | **New 2026-09-01, third pass:** this design assumed **datapacks were per world**, and was about to copy both ZIPs into every regenerated farm world | **wrong, corrected by measurement** on Paper 26.2 build 121. A probe pack in `<level-name>/datapacks/` was listed and enabled; an identical probe in a secondary world's own `datapacks/` folder was never seen — not at start, not after that world was created, not after `refreshPacks()`. There is no per-world datapack API: `DatapackManager` hangs off `Server`, and `WorldCreator` has no datapack option. The nightly farm world inherits both packs with nothing copied, and the plugin's job shrank from installing to verifying. The same drill produced the second surprise: a Bukkit-created world lands at `<level-name>/dimensions/minecraft/<name>`, inside the primary world rather than beside it |
| 19 | **New 2026-09-01, third pass:** Gradle does not track `.env.example` as an input of `:discord-bot:test`, although `ConfigsTest` reads it | **closed 2026-09-02**, after the premise itself turned out to be wrong: `ConfigsTest` did *not* read that file, it held a hand-copied literal of the language list and the price list, so the real problem was worse than the one written here — two copies with nothing comparing them. Both halves are fixed. The test now reads the `NORDTAL_ACCESS_LANGUAGES` and `NORDTAL_ACCESS_TIERS` values out of the real file, and `build-logic`'s `repositoryRootTestInputs` declares it an input of the test task; the same mechanism declares `compose.yml` for `:updater:test`, which had the identical hole. Verified by editing each file and watching both tasks re-run rather than report UP-TO-DATE, and by changing a price and a language tag in `.env.example` and watching the two cases fail. It also found a **second `.env.example`, in `discord-bot/`** — a leftover of the bot's own compose deployment, pinning `BOT_VERSION=0.1.0`, which the walk-up found first because it was nearer; it was deleted, and the lookup now anchors on the directory holding `settings.gradle.kts` rather than on the nearest file by name |
| 20 | **New 2026-09-01, fourth pass (deployment audit):** the published release `v0.1.0` carried a `smp-0.1.0.jar` and a `limbo-0.1.0.jar` of **51 279 bytes each** — the scaffold's two log lines — while the same version built from `main` that day was 4 820 904 and 4 576 946 bytes. Twenty-six commits sat between the tag and `HEAD`, two whole modules among them, and `.env.example` pinned exactly that release. Following the runbook to the letter would have started an SMP server with no SMP in it | **half closed.** The repository is on `0.2.0` and `.env.example` with it; publishing the release is Till's, in `todo.md` §7. The cause is not closed and cannot be by an edit: a pin has to be moved by hand, and the hand is busy building. That is what [updater.md](updater.md) is for |
| 21 | **New 2026-09-01, fourth pass:** the proxy's resource pack `url` and `sha1` appeared in **neither `compose.yml` nor `.env.example`** — a repository-wide search found nothing. `PackSpec` gives both empty defaults and `network-control` fails closed, so step 3 of the deployment runbook could not have worked, and that document's claim that `.env` is the whole configuration was false | **closed the same day.** Both are variables now; the URL derives from `SEASON_REPO`/`SEASON_RELEASE`/`SEASON_VERSION` and needs no line, `PACK_SHA1` is required by name. Verified with `docker compose config`: the URL resolves to the release asset, and omitting the sha1 fails with its own message before anything starts. **Superseded the same day by finding 23** — the values move back into `pack.yml` in step 3 of [updater.md](updater.md), where the updater writes them and nobody copies a hash out of a release at all |
| 22 | **New 2026-09-01, fourth pass:** nothing publishes the Minecraft image. `.env.example` suggested `#MC_IMAGE=ghcr.io/nordtal/minecraft:0.1.0` and `release.yml` pushes the bot image and no other | **closed, and it was closed by `release.yml` long before anybody wrote it down.** All four images - `minecraft`, `discord-bot`, `updater`, `postgres-backup` - are built and pushed by the release workflow to `ghcr.io/nordtal`, each tagged with the version and `latest`. Measured 2026-09-06: every one of them answers an **anonymous** manifest request with HTTP 200 and lists tags up to `0.5.1`. The half of this that became finding 28 - that a host building images needs a checkout - stopped applying the day the host stopped building anything |
| 23 | **New 2026-09-01, fifth pass (building the updater):** the fix for finding 21 cannot survive the module it was written for. `compose.yml` sets `NORDTAL_NETWORK_CONTROL_PACK_URL` and `..._SHA1`, and **a jcore environment override wins over the file and is never written back to it** — so an updater writing a new sha1 into `pack.yml` would write into a value nothing reads: a swap that reports success and changes nothing | **closed the same day.** Both variables are gone from `compose.yml` and `.env.example`, `SEASON_RELEASE`, `SEASON_PLUGINS` and `EXTRA_PLUGIN_URLS` with them, and `updater apply` writes `pack.yml` itself — taking the URL from the release asset and the hash from the `.sha1` asset beside it. Verified end to end on 2026-09-01 against the live APIs: Chunky 1.5.3 fetched, its sha512 matching Modrinth's published hash, the superseded 1.5.2 deleted, an unclaimed jar untouched, and `pack.yml` rewritten with its comments intact |
| 25 | **New 2026-09-01, sixth pass (steps 4-6 of the updater):** the updater was documented as a container that "must never be given a restart policy", and its own design said it would install its own new jar and come back on it after a redeploy. **Both could not be true at once.** Its Dockerfile baked the jar in with `ARG JAR`, so a redeploy brought the same image back with the same jar in it, and a container that only ever runs to completion cannot answer a button in Discord either | **closed the same day, by separating two things that had been one.** The rule was never about the container's lifetime — it is that *nothing updates on a schedule*. So `serve` exists and runs all the time with no timer and no watch in it, and the updater and the bot both run their jar out of a volume with the baked one as a floor for a first deployment. The wrong sentence was in `docs/updater.md`, `CLAUDE.md`, `updater/README.md` and the Dockerfile; all four say the right thing now |
| 26 | **New 2026-09-01, sixth pass:** an `apply` run where **every** row was skipped — an unmounted volume, a source that did not answer — closed its report with **"Nothing needed doing."** No work was done and nothing failed, so both existing branches were false and the remaining one was the dangerous reading | **closed the same day.** A third footer says the network is *not* known to be current and points at the skipped lines. Found on a real container run against the live APIs, not by a test; the regression test was written afterwards |
| 27 | **New 2026-09-01, sixth pass:** the updater applied the schema and *then* the rest of the stack started, with nothing ordering the two. After a redeploy every container starts at once, so a plugin could come up against a schema older than itself — which is the exact failure the module was built to prevent, reintroduced by the module | **closed the same day.** The updater touches `/tmp/updater-ready` once the schema is current, that is its healthcheck, and every other service has `depends_on: updater: service_healthy`. Safe where the deliberate absence of a database `depends_on` is not, because the updater has no profile: depending on it cannot drag in a service nobody asked for |
| 28 | **New 2026-09-01, seventh pass:** `compose.yml` lived in `deploy/` and had build contexts pointing *out* of it — `../updater` and `../discord-bot`. Arcane's GitOps sync pulls **only the directory the compose file lives in** (its own documentation), so a synced deployment would have had `deploy/` on the host and neither build context. The stack would not have built, and nothing said so anywhere | **closed the same day.** `compose.yml` and `.env.example` moved to the repository root, so the synced directory is the whole repository and every context resolves; the contexts became `./deploy/minecraft`, `./deploy/postgres-backup`, `./updater`, `./discord-bot`. The header of the compose file and `deploy/README.md` both say why, because "tidy it back into deploy/" is the obvious wrong move. Verified by rendering the file with `docker compose config` against a filled `.env`: all six contexts resolve to real directories |
| 29 | **New 2026-09-01, seventh pass:** `arcane.redeploy-path` defaulted to `/api/projects/{project}/redeploy` with `{project}` substituted by the *name* `nordtal-s2`. Reading Arcane's source showed the real route is `POST /api/environments/{id}/projects/{projectId}/redeploy` and that **both segments are IDs** — the project one a UUID Arcane generates. The documented guess was wrong on two counts, and its own 404 message told the reader to fix the path while saying nothing about the name | **closed the same day.** `arcane.environment` (default `0`) and `arcane.project` (no default, required once `base-url` is set) are separate settings substituted into the path; the 404 names both IDs and the call that lists them; `Configs` refuses a base-url without a project ID at startup, with a message that names `nordtal-s2` as the wrong value. Four tests, and the fail-fast was driven through the real jar |
| 30 | **New 2026-09-01, seventh pass:** [arcane#1943](https://github.com/getarcaneapp/arcane/issues/1943) — a redeploy of an **already running** project answering success and doing nothing. That is exactly our case: the stack is up when the button is pressed | **open, and it cannot be closed from here.** Reported on one agent at v1.15.3 and closed as *not planned*. No code in the updater can detect it: the NDJSON stream that would say what happened is one the redeploy kills this container part way through reading, which is by design. The check is a person watching the containers cycle on the first press, and it is in [`todo.md`](../../todo.md) §7 |
| 31 | **New 2026-09-02, eighth pass — and this one was found in production, not by reading.** `compose.yml` defaulted the Minecraft image to `ghcr.io/nordtal/minecraft:local`, a tag nothing has ever pushed, on the assumption that the host would build it from the `build:` block beside it. **Arcane deploys by pulling and never builds** — building is a separate action in its interface (its documentation, read 2026-09-02) — so the deploy failed with `error from registry: denied`. Two of the four images could not have been built there under any circumstances: they `COPY` a jar Gradle produces into a directory `.gitignore` excludes | **closed the same day.** `release.yml` now pushes all four images to `ghcr.io/nordtal`, each under the release version and `latest`; every image in `compose.yml` defaults to a `ghcr.io/nordtal` reference tagged from one `IMAGE_TAG` variable, so a rollback is one line. `build.yml` builds all four without pushing, so a Dockerfile that does not build fails on the commit rather than on the release. `TopologyTest` asserts the rule directly — an image of ours whose default is not a `ghcr.io/nordtal` reference fails the build, which is the test that would have caught this |
| 32 | **New 2026-09-02, eighth pass:** `serve` migrated the schema and then waited, so a Minecraft server always came up against an empty `plugins/` folder on a first deployment and stopped. Filling it was `docker compose run --rm updater apply`, **typed by a person with a shell on the host** — which an Arcane-managed deployment does not have. The stack could not reach a running state on its own, and no document said that was a requirement | **closed the same day.** `serve` installs what is `MISSING` before it writes the readiness marker, bounded by `UpdatePlan#onlyMissing()` so it can never move a version: an artefact that already has a jar keeps it, so a restart of a live network installs nothing. A failure there does **not** block readiness, deliberately — `serve` runs on every restart of a running network, and a GitHub outage during an ordinary redeploy must not take down four servers that were about to come back fine. Four tests on the filter; **never yet run against a real empty volume**, which is in [`todo.md`](../../todo.md) |
| 33 | **New 2026-09-02, eighth pass:** a GHCR package under an organisation is **private on its first push**, and a private package answers a pull with the same `denied` as one that does not exist. So fixing finding 31 alone would have reproduced the identical error message from a different cause | **closed - and it had been closed since 2026-09-02, in `todo.md`, four days before this row admitted it.** The owner set all four packages public that day and verified it anonymously (manifest *and* a real config blob, HTTP 200 without a credential, where half an hour earlier the same request had answered 401). This row went on saying "open, and only Till can close it" through every pass since, because nothing reads the checklist back into the findings table. Re-measured on 2026-09-06 rather than taken on trust a second time: an anonymous token from `ghcr.io/token` fetches all four manifests with HTTP 200 and lists their tags up to `0.5.1`. **The lesson is not about GHCR.** A finding closed in the owner's checklist and left open here is the same drift this section exists to catch, pointing the other way - and it took the owner saying "they are already public" to find it |
| 24 | **New 2026-09-01, fifth pass:** SnakeYAML read `sha1: 0000…` out of a `pack.yml` as the number `0`. Forty digits is a hash nobody will meet, but the same implicit type resolution mangles anything numeric-looking, and the damage is a comparison against a value that was never in the file | **closed the same day.** `PackState` parses with the implicit resolvers removed, so every scalar arrives as text. Found by a test using exactly that value as its "wrong hash", which is the only reason it was found at all |

| 34 | **New 2026-09-02, ninth pass — the first full `docker compose` deployment.** Two comments described the console as being mirrored with `tmux pipe-pane … > /proc/1/fd/1`: `deploy/minecraft/Dockerfile` and the `init: false` note in `compose.yml`. That is the one mechanism `deploy/README.md#never-mirror-the-console-with-tmux-pipe-pane` forbids outright, because it wedges the container past `docker rm -f` | **closed the same day.** Both now describe what the entrypoint does (`tail -F` on `logs/latest.log`) and name the trap. `:common`'s `EntrypointRulesTest` asserts the string `/proc/1/fd/1` appears in no executable line. The cause is worth keeping: a comment that describes the obvious-but-wrong implementation is the most effective way to get somebody to write it |
| 35 | **New 2026-09-02, ninth pass:** `compose.yml` carried a stale duplicate of the updater's header block — *"A COMMAND, NOT A SERVICE - no restart policy and no schedule"* and *"NOTHING PUBLISHES THIS IMAGE"* — fifteen lines above the correct block saying the opposite, and three lines above the `restart: unless-stopped` it denied. Finding 25 says the wrong sentence was corrected in four places; this was a fifth nobody looked at | **closed the same day** by deleting it. Both claims had been closed already, by findings 25 and 31 respectively |
| 36 | **New 2026-09-02, ninth pass:** **Nordtal's world seed was recorded nowhere** — not in `docs/smp.md`, not in `compose.yml`, not in `.env.example`, not in `todo.md`. The one occurrence of a seed anywhere in the workspace is `docs/hunger-games.md`, and that world is hand-built and shipped as a folder, so its `level-seed` is never consulted by anything. Nordtal is generated on the host, once, to border 4000, and then frozen for the season | **closed the same day.** `SMP_LEVEL_SEED` (default `1837371427`, the owner's choice) is seeded into `server.properties` while the world does not exist yet, and an existing world is compared and warned about rather than re-rolled. Recorded in `docs/smp.md#worlds` with the two caveats that make it usable: the datapack versions decide what the number produces, and it is the same value the hunger games records without the two worlds looking alike. **This is finding 17 again** — a dependency living in the operator's head rather than in a file — three days later |
| 37 | **New 2026-09-02, ninth pass:** `entrypoint.sh` set `remain-on-exit` on the session *after* `new-session` had already started the JVM. That works for every server that runs for a while and fails for the only one where the exit status matters: a JVM that dies at once takes the session with it before the option applies, every `display-message` falls back to `|| echo 1`, and the container reports status 1 whatever really happened — which is exactly what the crash-looping container in the first deployment reported: `server exited with status 1`, on a loop, with the real cause in no log anybody could reach | **closed the same day, and measured both ways in a container:** a real exit status of 3 came back as 1 before and as 3 after. The options are now set on the server before the first session exists, which needs `exit-empty off` because a tmux server with no sessions exits immediately; `pipe-pane` moved into the same `tmux` invocation as `new-session`, because a separate call against a pane that already died fails with *"target pane has exited"* and takes the crash output with it. `EntrypointRulesTest` asserts both orderings |
| 38 | **New 2026-09-03, on the running stack, and the first finding a real Minecraft client produced.** A player who had linked their account and applied the resource pack **sat in `limbo` for ever**, watching the pack title refresh on a loop, surviving every rejoin. Nothing was in any log — not a warning, not the release line, not a timeout — because the one path that discards a `nordtal:limbo` message without logging is the one that was taken. `limbo` sends `READY` **once per join**; `PackStation` recorded it in an object created by `ServerPostConnectEvent`, and Velocity dispatches the two with no ordering between them. Read `TransitionSessionHandler#handle(JoinGamePacket)` in Velocity 4.1.1: it stops reading from the backend, awaits `ServerConnectedEvent`, then resumes reading **before** firing `ServerPostConnectEvent` — so a `READY` sent one tick after the backend's join is already buffered and is handled first. It found no object, was dropped, and was never re-sent. Once the pack is applied no timeout applies and no other condition can end the wait, so the deadlock was permanent and silent | **closed the same day.** The three facts now live in `WaitingBook`, per session rather than per visit, and every handler records one and re-asks the whole question; `WaitingBookTest` drives **all six orderings** and fails on the old semantics (verified by putting the bug back). A second Velocity path loses a `READY` outright — one decoded in the same read batch as the join is written to the *client* by that same transition handler, which never consults the channel registrar — so the book also releases a player once everything else has been settled for `gate.yml#limbo-ready-grace-seconds` (5 s), logging a `WARN` that names the channel. **No single message may be able to strand a player.** An early `READY` is now logged at `INFO`, which is the evidence whose absence made this invisible. Still to be confirmed against a real client: `todo.md` |
| 39 | **New 2026-09-03, on the running stack.** The restart countdown announced **every successful restart as cancelled**, and `restart.now` was unreachable. `RestartWatch` polls for a `PENDING` restart every five seconds; the updater sleeps until exactly `not_before` (`nextDue()`) and claims the row on the instant. So the last poll sees a second or two left, the row goes `PENDING -> RUNNING`, and the next poll finds nothing pending with the counter above zero — which the old rule read as a withdrawal. The player watching a working restart was told *"The restart was called off. Nothing is happening."* `CountdownTest` had a case named `reachingZeroIsNotCancelling` whose comment says exactly this danger, and it passed: it asserted the path where a poll actually observes zero, which is a window a fraction of a second wide and never happens | **closed the same day.** The row does not vanish, it changes status, so `Countdown#gone` takes it: `CANCELLED` says called off, `RUNNING`/`DONE` says the restart is happening, `FAILED` gets a fourth line that did not exist — the countdown ran out and it did not happen, which is a different thing to be told. `RestartWatch` reads the row back by id on the one pass where a countdown ends. Three new cases, two of which fail on the old rule |
| 40 | **New 2026-09-03, on the running stack.** The first restart anybody asked for failed **forty milliseconds** after the POST, against `http://localhost:3553`. Arcane was up and serving that address in a browser the whole time — but the updater is a container, and `localhost` inside it is that container. `arcane.base-url` is the one setting whose correct value differs between the browser it is copied from and the process that uses it, and nothing said so: the row read *"Could not reach Arcane"*, which is true and useless | **closed the same day.** `Arcane#loopback` names it at startup as a warning and again in the sentence written into `update_request.result`, with the three ways out. `compose.yml` maps `host.docker.internal:host-gateway` on the updater so the documented answer actually works on Linux; `.env.example` and `updater.yml`'s own comment carry the trap. **Warned, not refused** — the updater is the only process that migrates, and refusing to start over an optional restart button would trade a working schema for a broken one. `ArcaneLoopbackTest` pins every spelling, including the exact URL that cost the evening |
| 41 | **New 2026-09-04, from a screenshot of a real client — the review's first finding, and the one that explains the other five.** The SMP's boss bar drew **the red admin nametag inside its background bar**. `grep -rn "font("` over the whole repository returned **nothing**: not one component anywhere ever named a font. Every `nordtal:bossbar` code point therefore resolved against `minecraft:default`, where the three fonts allocate independently — `BOSSBAR_BG_4` and `TAG_ADMIN` are both `U+E004`. **A missing font key is not a glyph that fails to draw; it is a glyph that draws something else**, which is why four days of screenshots read as "the pack is incomplete" | **closed the same day.** `Glyphs.FONT_BOSSBAR` / `FONT_BOARD` as plain `String`s (no Adventure type reaches `Glyphs`), a `bossBarText(...)` helper in both renderers, and `BossBarFontTest` reading the two sources as text — they are Bukkit-facing, so there is no JVM here that can build a boss bar |
| 42 | **New 2026-09-04.** The nametag over a player's head showed **DisplayTags' stock format**, not ours, and the integration that was supposed to write it had two independent bugs. `PlayerSurfaces` wrote the lines during `PlayerJoinEvent`; DisplayTags creates the tag on `PlayerClientLoadedWorldEvent`, which is later, and `NameTagManagerImpl#createNameTag` **removes the old tag and constructs a new one whose constructor applies `config.getLines()`** — so our lines were written and then thrown away. Underneath that, we serialised with `LegacyComponentSerializer.legacySection()` while `ComponentUtil#render` deserialises **MiniMessage**, so the colours would have been lost even with the ordering fixed. An earlier pass of this review reported the integration as *absent*; that was a truncated `grep` and is corrected here rather than deleted | **closed the same day.** `PresenceListener` handles `NameTagCreateEvent`, which fires from inside the creation and therefore has no ordering left to lose, and covers every path that makes a tag — join, world change, reload. `PlayerSurfaces#applyTo` serialises with MiniMessage |
| 43 | **New 2026-09-04.** The tab list had **no header and no footer on any server** — `player.playerListName(...)` was the only tab-list call in the repository | **closed the same day**, and in `:common` rather than three times: `eu.nordtal.s2.common.hud.TabList` composes it, the wording lives in each module's own bundle under the deliberately unprefixed `tab.header` / `tab.footer`, and `TabListTest` holds the three bundles together. **limbo's footer is asserted as different**: everybody there is hidden from everybody else, so a player count would sit directly over a list of one name. The logo is passed as `{logo}` from `Glyphs`, never written into a `.properties` file, and the test forbids private-use characters in the header |
| 44 | **New 2026-09-04.** There was **no way to change a message without a release.** `Messages.load` read the classpath and nothing else: no file on disk, no reload, and every value went to the player as literal text | **closed the same day.** A second layer merges `plugins/<name>/messages/<lang>.properties` (`config/messages/` for the bot) **key by key** — file-by-file would freeze the wording at the day it was copied and every later release's new keys would reach players as their own key names. `Messages` swaps a volatile map, so a reload reaches every listener, HUD and command that was handed the object at startup. An override naming a key no bundle declares is stored and never looked up, which is silent, so `unknownOverrideKeys()` collects them and all six modules log them by name. Five reload commands: `/smp reload`, `/hg reload`, `/limbo reload` (that module had no command at all before), `/network reload` on the proxy, `/access reload` in Discord (`/messages reload` for one day). **None of them re-reads a YAML config**, and each says why in its own class: a running border, a running waiting world and a running connection pool are not things to swap under the people standing in them |
| 45 | **New 2026-09-04.** The German bundles of `smp` and `hunger-games` were written in **ASCII transliteration throughout** — `Schluessel`, `benoetigt`, `Fuehre`, `Bestaetigungsfenster`, `geschuetzt`, `Bloecke`, `hoechstens`, `Schliessen`. `hunger-games` had **zero** German characters in thirty-six lines. `limbo`, `network-control` and `discord-bot` were clean, which is the tell: those two are exactly the modules whose text can land in the boss bar font (finding 46), and the workaround had spread from the HUD keys into chat lines that render in `minecraft:default` and never needed it. Encoding was never the cause — `Messages` reads UTF-8 explicitly and says so | **closed the same day**, 45 lines across the two files |
| 46 | **New 2026-09-04.** `nordtal:bossbar` carries **its own ascii sheet**, so a character the vanilla font has is not a character that font has — and it covered 166 characters, **not one of them ä ö ü Ä Ö Ü ß**. Nothing was broken yet, because no German HUD string contained an umlaut; that was the transliteration in finding 45, not luck | **closed the same day.** `resource-pack/tools/draw_ascii_extras.py` draws the seven at their **canonical positions in Minecraft's own ascii map** — proved rather than assumed: the cells already drawn in rows 9, 10 and 14 land exactly on the vanilla CP437 slots for `£`, `ƒ`, `ª`, `º`, `¬`, `«`, `»`. Coverage is 173. `ResourcePackTest` asserts the seven by name **and** every character of every `smp.hud.*` / `hg.hud.*` / `smp.world.*` string against the font |
| 47 | **New 2026-09-04.** **Three message formats ran side by side and nothing compared them**: `hunger-games` carried legacy `§` codes, one `network-control` key wrote MiniMessage tags, and the other five hundred lines were plain text wrapped in `Component.text(...)`. Each is invisible to the other two, so the way a mismatch reaches a player is `§l` or `<bold>` printed on their screen. `hg.win.player` was the live example: `§l{winner}§r`, in the one module whose bundles nothing parsed | **closed the same day.** 98 call sites in 30 classes now go through `MessageRenderer`, which parses MiniMessage and **escapes every substituted value** — a player, world or milestone named `<red>` cannot colour the rest of the line. `hg.win.player` is `<bold>`; `Placeholders.escape` in `network-control` was a second implementation of that one security rule and now delegates to the first. `OneMessageFormatTest` pins both rules and carries a **named allowlist** of the files that still compose by hand, each with its reason — three of them marked OUTSTANDING (finding 48) |
| 48 | **New 2026-09-04, and open.** The **GUIs and the boards still compose their text in Java**: `NavigateGui`, `ObjectiveGui`, `BalloonGui` and `Boards` take a message, concatenate a POI name, a player name or a glyph progress bar onto it, and style the result with `.color(...)` in code. So an operator's override of `smp.objective.<key>` carrying a tag reaches the menu as literal text, and the styling of those surfaces is the one thing a release is still needed for | **closed 2026-09-06.** All four take their colour from the bundle (`<gray>`, `<green>`, `<white>` … as tags in `de`/`en`) and pass the variable half - a milestone's name, an objective's, a POI name a player typed, a world's - as a parameter through `MessageRenderer`, which escapes it. Nine new keys, two of them the objective-board row in its two colours. `OneMessageFormatTest`'s allowlist carries no `OUTSTANDING` entry any more, and every inventory title and lore in `smp` is now something an operator can restyle without a release |
| 49 | **New 2026-09-04, and open.** **`smp_duel` has no writer.** `V6__smp.sql` creates it with two carefully paired constraints — `smp_duel_ended_has_winner`, whose comment explains that a half-written outcome is what would make "the aura books disagree with the duel history" — and **no class anywhere reads or writes the table**. `Duels` books the aura through `smp_aura_event` and forgets the duel. So there is no duel history for the constraint to disagree with | **closed 2026-09-04.** The owner chose the second: `V10__drop_smp_duel.sql` drops the table, and the two indexes go with it. A duel's aura is two `smp_aura_event` rows and is untouched; what is gone is the ability to answer "who won how often", which nothing asked. Verified against a real PostgreSQL container — `:common`'s 206 tests apply V1–V10 in order, none skipped |
| 50 | **New 2026-09-04, and open.** `docs/smp.md` §"the milestone track" says a milestone unlocking is a **server-wide event — "title and chat announcement in every player's language, announced in Discord as well"**. `ObjectiveEngine#announceMilestone` sends **a chat line**. There is no title and there is no Discord announcement. The German string even says *"Schau nach oben"* — look up — at a sky where nothing happens | **closed 2026-09-04.** All four halves happen now: the chat line, a title held three seconds, `BIG_SUCCESS` for whoever closed it against `NETWORK_EVENT` for everybody else, and three rockets up around **every** player, wherever they are standing - the season has no one place everybody is. **The German line is unchanged and was made true instead**: *Schau nach oben* now has something to see. The rockets are stamped in their persistent data and `WorldEffects#onDamage` refuses damage from a stamped one, because a rocket with explosion effects damages whatever is near it whoever launched it, and bursting high is only safe until the first player under a ceiling. The Discord half of the sentence is finding 52 and is still open. The balloon half always held: `BalloonGui` reads `season.unlocked()` live |
| 51 | **New 2026-09-04, and open.** `docs/smp.md`'s farm-reset sequence says the warnings go out at 30 / 10 / 5 / 1 minutes **"chat + HUD"**. `FarmWorldReset#warn` sends chat only; `SmpHud` has a status bar and a navigation bar and no third line | **closed 2026-09-04, and the HUD was the half that moved.** `SmpHud#announce` takes the status line over for eight seconds, keeping the dimension icon, and `FarmWorldReset#warn` calls it at each of the four warnings alongside the chat line and the `COUNTDOWN_TICK`. Eight seconds rather than a takeover: warnings at 30 and 10 minutes would otherwise hold a player's dimension and milestone hostage for twenty minutes to say something that has not changed. The bar's text is its own short key (`smp.hud.farm-warning`, four words) because 182 px is the whole bar and the sentence belongs in chat, where it can be scrolled back to |
| 52 | **New 2026-09-04, and open.** `docs/smp.md` §"Discord" says *"The bot gets a narrow SMP surface — announcements, logs, and commands for admins and users."* **The bot has no SMP surface at all**: no announcement, no log, no command. Its eight slash commands are access, phase, update and messages | **closed 2026-09-06, on the wire that already existed.** `announce <language> <text>` is a `Target.BOT` command on a new `Surface.SYSTEM` that no adapter registers: the SMP renders a milestone's completion and each farm-reset warning in every language it has a bundle for and submits one `command_request` row per language (`Announcer`, source `CONSOLE`, requested by `smp`, kept an hour so a bot that is restarting still posts); the bot's inbox runs it and posts into `languages[].announcement-channel`, the second optional id in `access.yml`. A phase change is announced by the bot itself from the status tick that already reads the phase every minute. `/smp status` - phase, active milestone with progress, who is on - is the one `/smp` command a player may run, from Discord as a row. No V12 and no second NOTIFY channel, which is what the owner asked to be checked first |
| 53 | **New 2026-09-04, and open.** **There is no sound and no particle anywhere in this repository.** `playSound`, `Sound.`, `spawnParticle` and `Particle.` return zero hits across all seven modules. A season with a wheel of fortune, a hot-air balloon, duels, graves, milestone completions, a shrinking border and a battle-royale countdown is entirely silent. `showTitle` appears in exactly one game surface, `limbo`'s waiting room — the least interactive server on the network is the only one that uses the screen | **closed 2026-09-04, in two halves on the same day.** The sound half is a ten-constant `Feedback` vocabulary in `:common` mapped per module through a reloadable `sounds.yml`, wired at some forty call sites in `smp` and `hunger-games`, with `SoundVocabularyTest` refusing a call site that names a sound - written while the answer was still zero call sites. The world half is `smp`'s `WorldEffects`: the milestone ceremony, the balloon at both ends, a grave being opened and the duel arena, with `WorldEffectVocabularyTest` making the same rule about particles. **The two are deliberately asymmetric** - the sounds are config and the effects are code, because ten categories over forty call sites compress and four effects over four call sites do not; the reasoning is in `docs/presentation.md` section 6 so that the next session does not "finish" it into a config file. `showTitle` now appears in the milestone ceremony as well as in `limbo`. **None of it has been heard or seen**: that is the owner's checklist |
| 54 | **New 2026-09-04.** **Eight commands exist in no document.** `/smp farmreset`, `/smp objective complete`, `/smp milestone unlock`, `/smp aura`, `/poi add`, `/poi remove`, `/hg ready` and `/hg ready-status` are implemented and reachable, and `grep` finds none of them in `docs/`. `docs/smp.md` describes the three escape hatches as a design feature over two paragraphs and **never names the command that operates any of them**, so an admin reading the documentation cannot use the feature it argues for | **closed 2026-09-06.** All eight are in the command table in [`docs/README.md`](README.md), with the two-step rule marked on the three that need it, and the five reload commands added by finding 44 are in the same table. This row said "open" for two days after the table was written, which is the drift the section above is about |
| 55 | **New 2026-09-04, and open.** **No Minecraft service and not the bot has a healthcheck.** Only `postgres` and `updater` have one. `CLAUDE.md`'s justification for `getServer().shutdown()` on a fatal config reads *"every jar is in `plugins/`, so the entrypoint's guard passes, and the port is open, so the healthcheck passes"* — describing a mechanism that is not in `compose.yml`. The rule it argues for is right and stays; the sentence quotes something that does not exist, and the real situation is worse than the one it describes, because nothing outside the JVM reports anything at all | **closed 2026-09-04 — and half of this finding was wrong.** `deploy/minecraft/Dockerfile` has carried a TCP `HEALTHCHECK` since 2026-09-02, so the *container* did report health; what `compose.yml` lacked was a healthcheck of its own, and a compose one replaces the image's outright. So "the sentence quotes something that does not exist" was **false** — it quoted the image's check, accurately — and "nothing outside the JVM reports anything at all" was false too. The finding that survives is the smaller, real one the Dockerfile's own comment already stated: *"a jar that is present and whose onEnable threw"* is invisible to a port test. Now five processes write `/tmp/nordtal-ready` at the end of a successful start and refresh it every 30 s; `compose.yml` tests its **age**, AND-ed with the port for the Minecraft services so the image's guarantee is not traded away. `Readiness` is platform-neutral in `:common`; `ReadinessWiringTest` catches the beat moving above a refusal or onto the wrong thread, and `TopologyTest` holds the shell's `-lt 90` equal to `Readiness.STALE_AFTER`. The healthcheck strings were driven against throwaway containers — fresh marker healthy, missing marker unhealthy, 200 s-old marker unhealthy. What is unproven is that a real Paper start reaches the call at all: `todo.md` |
| 59 | **New 2026-09-04, found while wiring the heartbeat.** **The bunq client sets no HTTP timeout anywhere.** A call that hangs blocks the bot's timer thread indefinitely, and nothing bounds it. This is why the heartbeat deliberately shares that thread — a wedged bunq call is a bot that has silently stopped booking payments, and sharing the thread is the only thing that surfaces it | **closed 2026-09-06 by measurement, and the finding was half wrong.** This class sets no timeout; the SDK does - `com.bunq.sdk.http.ApiClient` (1.28.0.6) builds its OkHttp client with connect, read and write timeouts of `TIMEOUT_SECONDS = 30`, read out of the shipped bytecode with `javap`. So a bank that stops answering costs the worker thread thirty seconds per call, which is bounded and visible through the heartbeat that shares the thread. Written on the class; no wrapper, because one would bound nothing the SDK does not already bound |
| 60 | **New 2026-09-04, found while wiring the sound vocabulary.** **Velocity cannot play a sound the ordinary way.** `velocity-api:4.1.1` documents `Player#playSound(Sound)` and the coordinate overload as *"not currently implemented in Velocity and will not perform any actions"* — read from the 4.1.1 sources jar, not from memory; both are empty default methods. Only `playSound(Sound, Sound.Emitter)` works, on 1.19.3+, and it "requires a present `getCurrentServer()` for the emitting player as well as this player" | **open.** `Player` is itself a `Sound.Emitter`, so `player.playSound(sound, player)` is the shape that could work for anybody already on a backend — **untested, and only a real client can test it.** Until then a restart countdown from the proxy is chat and title only, which is what it is today. It also means `NETWORK_EVENT` on a phase switch cannot come from the proxy: the backends have to play it |
| 61 | **New 2026-09-04, found and fixed the same day.** **The sounds were a `sounds:` block inside `smp`'s `config.yml`**, whose documented escape hatch is "blank the key to silence a category". `config.yml` is deliberately not reloadable — the plugin binds worlds, borders and coordinates once at enable — so using that escape hatch would have cost a **restart of the season**, and the `todo.md` item telling the owner to blank a key did not say so | **closed 2026-09-04** — `sounds.yml` is its own file with its own handle, `/smp reload` re-reads it, and `SmpSounds` holds a volatile reference so the one instance fifteen listeners were handed at enable answers the new file from the next click on. `SoundDefaultsTest` drives exactly that; `ConfigsTest` asserts by name that `config.yml` now *refuses* a `sounds:` block, because the reason it must keep refusing one is invisible from `SmpSpec` |
| 62 | **New 2026-09-04, found while wiring `hunger-games`' sounds and fixed the same day.** **The post-game ceremony queried the database on the main thread, once per member *per player*.** `Ceremony#announce` ran a `killCount` per participant inside a loop over every online player - forty in front of forty is **1 600 blocking queries on the server thread** at the single moment the whole event ends - plus `decideGame` and `activeMembersOf`. Every one of those 1 600 returns the same answer: the tally does not depend on who is being told | **closed 2026-09-04** — `Ceremony` no longer holds the DAO at all, which is the only version of the rule that cannot quietly come undone. One grouped `killCounts(gameId)` replaces the loop, and it, `decideGame` and the roster all happen in the async block `CombatListener` already had; the ceremony receives a `Ceremony.Decision` and runs no query. `KillCountsIntegrationTest` (4) drives the new query against a real PostgreSQL - `count(*)` is `bigint` against a `Map<UUID, Integer>`, and `@KeyColumn`/`@ValueColumn` name columns as strings nothing else checks |
| 63 | **New 2026-09-04, found while building the first menu.** **`docs/presentation.md` said "a test asserts no menu opens a non-chest inventory", and no such test existed** - the sentence was written the day the style sheet was, and read ever since as a statement about the repository. It is the same failure this document opens by warning about, and the third standing correction of its kind here. The rule itself matters: a chest window is `114 + 18*rows` and all six sizes are even, which is the whole reason one panel per row count lines up; a hopper is 133 | **closed the same day.** `ChestOnlyMenuTest` in `:common` asserts it over the four client-facing modules' sources, and asserts a second rule beside it - every inventory title goes through `MenuTitle` - with a named allowlist of the four menus still waiting for their panel, so what is left of the menu pass is in the build rather than in a document. It carries a non-vacuity anchor: the scan has to find `NavigateGui`, or it is passing on an empty set |
| 64 | **New 2026-09-04, found the day after writing it.** **`AdminOperators#refresh` had no caller.** It was written, documented and covered by three of `AdminOperatorsTest`'s eleven cases on 2026-09-04, for the express purpose of removing operator from an admin whose Discord role was revoked while they were online — and nothing called it. So the shipped behaviour was the one it was written to replace: the flag read once at join, operator kept until the account chose to disconnect. This is the worst of the three possible states, because a mechanism that exists and is tested reads as a mechanism that works, and the question it answers ('is this person still an admin?') looks identical when nothing is answering it | **closed the same day.** `:paper-common`'s `AdminWatch` is its caller, on all three backends: a poll (`config.yml#admin-poll-interval-seconds`, the guarantee) and a `LISTEN nordtal_admin` connection (comfort, switchable off). The reconnect loop moved into `:common` as `eu.nordtal.s2.common.notify` on the way, because writing it a fourth time was the alternative. `AdminWatchWiringTest` is what stops it recurring — a text search, in the shape of `ReadinessWiringTest`, asserting all three plugins build one, start it and close it |
| 65 | **New 2026-09-04, and it is finding 64's twin.** **`:commands` had no command in it.** The module was scaffolded that morning - `NordtalUser`, `Declaration`, `Argument`, `Values`, `NordtalCommand<E>`, sixteen tests - and nothing implemented the interface. A search for unreferenced classes across the whole repository found exactly two: the deliberate bunq classpath shadow, and `NordtalCommand`. A module that exists, compiles and is tested, and that no process calls, is indistinguishable from one that works | **closed the same day** by folding `/phase` into it - chosen as the proof because it is the only command that existed on two surfaces, so the layer could be shown to make one implementation out of two rather than a third beside them. What the two had drifted into is the finding underneath the finding: the bot answered in hardcoded English while the proxy answered in the asker's language, the bot confirmed a switch and the proxy did not, and only one of them reported that moving `smp_start` had shifted other people's paid access. 22 tests now assert what the command decides, without a proxy and without a guild |
| 66 | **New 2026-09-04, and it is the finding the whole command survey turned on.** **The console could run no part of `/hg`.** Every subcommand carried `.requires(source -> source.getSender() instanceof Player)` and every handler opened with a cast to `Player`, so the start of the season's flagship event depended on one client being able to connect *and stay connected* - with no second path and nothing anywhere saying so. `/phase` has had a documented fallback for exactly that failure since 2026-08-31, and it is the less important of the two commands | **closed the same day.** The handlers take a `NordtalUser` (`:paper-common`'s `PaperUser`), so a player and the console are the same thing to all of them; `/hg ready` stays player-only because it marks *the sender* ready. The console is an admin without a database read - the single exception to `discord_user.admin` being the only admin list, and it is one because a container shell can edit that table by hand. `ConsoleUsableTest` (2) counts the Brigadier gate so that re-adding one is a build failure rather than a silent two-character regression. **Unrehearsed**: no test here starts a server, so "the console actually reaches the handler" is in `todo.md` |
| 67 | **New 2026-09-05, and it is what the fold was for.** Four of the bot's admin commands - `/grant-access`, `/revoke-access`, `/access-status`, `/settle` - checked `discord_user.admin` **nowhere at all**. Their only gate was Discord's own `DefaultMemberPermissions.DISABLED`, which is a Discord permission and not this network's admin list, so the set of people who could grant paid access was whoever a server administrator had given the right Discord permission to. `/settle`'s autocomplete had no check either, so anybody who could see the command could enumerate every open payment reference in the network | **closed** - all four are declarations now, `adminOnly` is a property of the declaration, and every adapter checks it. `AccessCommandsTest` asserts it for all six. Found by writing the second implementation next to the first, not by a test |
| 68 | **New 2026-09-05.** Every line those four commands printed was **hardcoded English**, built with a `StringBuilder`, in a bot whose whole message system exists so that nothing is - nine of them in `/access-status` alone. docs/architecture.md has called that a bug rather than a shortcut since 2026-08-31 | **closed** - they are message keys in `:commands`' own bundle, in both languages |
| 69 | **New 2026-09-05.** `/smp aura` answered an unlinked target with `smp.error.no-account-link` - "**Your** Minecraft account is not linked" - which is written for a player about their own account and told an admin the wrong thing about the person standing in front of them | **closed** - `smp.admin.target-unlinked`, addressed to the admin and naming the player. `SmpCommandsTest` covers it |
| 70 | **New 2026-09-05.** `/limbo reload` rendered its reply against `player.locale()`, the **Minecraft client's own setting**. docs/i18n.md says in as many words that a player's language is `discord_user.locale` and that the client's setting is consulted nowhere in this repository | **closed** - it goes through `PlayerLocales` like every other reply |
| 71 | **New 2026-09-05.** `/grant-access` had **no upper bound** on days. It hand-checked "greater than zero" in the handler and nothing else, so a mistyped `3650` was a decade of free access and one keystroke away from `365` | **closed** - the bound is on the declaration (1-365), so Brigadier, Discord's own option validation and the request row all refuse the same numbers |
| 72 | **New 2026-09-05, and it was almost written in.** `hunger-games` briefly passed `dao.isAdmin(uuid)` as its command tree's admin source. Brigadier evaluates `requires` **while building the tree it sends to a client**, so that is a database round trip on the main thread, per player, per rebuild - the rule this repository has held since 2026-09-01. `limbo` came within a line of the mirror-image mistake: `FullServerAdmission`, which does hold an admin flag but only fills it at pre-login **when the server is near its cap**, and `limbo` never is - it would have answered "nobody is an admin" for ever, silently | **closed before it shipped** - both read `AdminWatch#isAdmin`, the same set the operator grant is applied from, and `AdminSourceTest` pins the allowed source per plugin and forbids both wrong ones by name |
| 73 | **New 2026-09-05.** `hunger-games` and `limbo` loaded only their own message root while their command keys had moved into `:commands`' shared bundle. `Messages` degrades to the key rather than throwing, so `/hg start` would have answered with the literal string `hg.start.started` - and nothing else could see it: both bundles were internally consistent, both languages carried the same keys, and the command named a key that did exist. It is only wrong at the seam | **closed** - both load the shared root first, and `SharedBundleLoadedTest` walks all five processes and asserts it, because moving keys is exactly the change somebody makes while thinking about something else |
| 56 | **New 2026-09-04.** `CLAUDE.md`'s list of migrations jumps **V6 → V8**, and `V7__update_request.sql` and `V9__smp_start.sql` are both on disk and both applied. This document already mentioned V7; the module guide did not | **closed the same day** by writing both in |
| 57 | **New 2026-09-04.** `CLAUDE.md` says `Glyphs` *"is knowingly behind the table today — it still declares the four retired season-1 role tags and names nothing at all from the boss bar font."* **Both halves are false.** It declares one tag and one badge, and it names the entire boss bar font: nine background segments, thirteen icons, sixteen arrows and fifteen space advances. The sentence describes the state on the day somebody wrote it, which is the failure mode this document opens by warning about | **closed the same day** |
| 58 | **New 2026-09-04, closed the same day.** **`nordtal:board` was fully drawn and used by nothing.** Twenty-two textures, twenty-eight code points, corners, edges, dividers and its own negative-advance space provider — and `Boards.java` wrote plain text onto a Text Display without touching one `BOARD_*` constant | **closed:** `:common`'s `BoardFrame` draws it, `Boards` calls it, `BoardFrameTest` (11) holds the arithmetic against the PNGs rather than against its own constants. The width could not be computed the way the fallback in `todo.md` assumed — the board's text renders in `minecraft:default`, whose advances live in the client — so the owner decided on 2026-09-04 that it is configuration (`boards[].width`, 32–240 px) and an over-long line draws over the edge rather than wrapping. The font gained six positive advances for it. Whether it *looks* right is a rehearsal item |
| 74 | **New 2026-09-05, from the CodeRabbit review of the command layer.** A `CHOICE` argument was **not checked against its own choices on either chat surface.** Discord builds a dropdown and its client refuses anything else; `RequestArguments#decode` refuses one coming off a request row; Brigadier has no enum type, so `PaperCommands` and `VelocityCommands` both typed a choice as a plain word and took whatever was typed. `/hg start` is what made that expensive: the trailing `confirm` is a choice and `StartGame` treats *any* present value as the second step, so **`/hg start yes` would have spent an armed confirmation and begun the season's flagship event below the recommended minimum**, having said nothing about the word it did not understand | **closed** - `NordtalCommand#check` runs the generic checks and then the command's own `problem()`, in one place all three adapters already called; `ChoiceCheckTest` covers it |
| 75 | **New 2026-09-05, same review.** **`/hg start confirm` was refused on a healthy game.** The trailing word alone made it a confirmation, so on a registration at or above the soft minimum nothing had ever armed one, `consume` missed, and the admin read "that confirmation window has passed". Nothing had expired and the event did not start - an admin who always types the second step could not start a healthy game at all | **closed** - the confirmation is only required when the count is below the soft minimum; two new `HungerGamesCommandsTest` cases |
| 76 | **New 2026-09-05, same review.** **`/hg start` said the event had begun before starting it.** The success reply went out first, `effects.start(...)` ran last and carried no `catch`, unlike the registration read above it - so a start that threw left the admin told it had worked and the game recorded as started | **closed** - start first, then the reply, with `hg.start.failed` on the failure path saying explicitly that this is not a case for typing the command again |
| 77 | **New 2026-09-05, same review, and the guard was inverted.** `/phase show` suppressed `phase.read.failed` **exactly on the path that had no other answer.** A process that caches nothing - the bot, and a request claimed off a row - would have said nothing at all: a Discord interaction with no response, and a `command_request` row settling empty | **closed** - it always answers, with a second key (`phase.read.failed.only`) for that path, because the original sentence says "the phase above" and on that path there is nothing above |
| 78 | **New 2026-09-05, same review.** **A greedy argument that began or ended with a space did not survive the request row.** `decode` walks past the spaces between arguments before reading the greedy one, so `encode(" 2026-10-01 18:00")` came back stripped - the far side ran the command with a different value and nothing said so. A pasted date reaches that path. Two more of the same shape: a value supplied after an *absent* optional was dropped in silence, and `Character.isDigit` accepted Devanagari digits for a Discord snowflake | **closed** - all three throw now, and `RequestArgumentsTest` gained a case each |
| 79 | **New 2026-09-05, same review.** **A Discord confirmation button could be pressed twice.** `editComponents().queue()` is a REST call: it returns immediately and the button stays live until Discord applies the edit, so a second press inside that round trip arrived as a second event and both reached the command. On `/smp farmreset now` that is two world deletions | **closed** - the message id is claimed in memory before the work starts |
| 80 | **New 2026-09-05, same review.** **`/access settle`'s autocomplete was gated on nothing.** Discord's own `DefaultMemberPermissions` decides who *sees* the command, and this whole change exists because that is a different list from `discord_user.admin` - so anybody a server administrator had given the right Discord permission to could enumerate every open payment reference. It was deliberate ("a query per keystroke"), which is the wrong trade | **closed** - checked against a ten-second per-id cache, so typing a six-character reference is one query rather than six |
| 81 | **New 2026-09-05, same review.** **`/access status` answered normally for a member who had left the guild**, because it resolved the asker through JDA's *global* user lookup rather than the configured guild's member list - and that lookup throws for an id nobody has, so the `null` branch under it was unreachable | **closed** - `AccessRoles#member` resolves through the guild and maps absent, unknown and not-a-snowflake to the same empty answer |
| 82 | **New 2026-09-05, same review.** **The inbox rendered remote answers from a bundle that was never reloaded and carried no operator override.** So `/x reload` moved the wording a command said in chat and not the wording the same command wrote back into a request row, and an override the operator wrote reached one and not the other. The bundle is deliberately unlayered - the module's own is allowed MiniMessage, which a Discord admin would read as a literal `<green>` - but *unlayered* is not *override-free* | **closed** in all five processes: the shared bundle now carries the override directory and is reloaded next to the module's own |
| 83 | **New 2026-09-05, same review.** `PaperCommands` resolved an `ACCOUNT` argument through `account_link` **inside the Brigadier handler**, which is the main thread - the rule this repository has held since 2026-09-01. Also: `AdminWatch` stored and published the DAO's own set rather than a copy, so a consumer that kept and mutated it would change who the server treats as an admin; `suggest()` accepted suggestions for kinds that carry their own and dropped them silently; `/smp milestone unlock` suggested a milestone track captured at enable, so `/smp reload` never reached it; and `BukkitSmpEffects#nameOf` called `Bukkit.getPlayer` off the main thread | **all closed** - the account lookup hops off the thread and the command hops back onto it, and the other four are one line each |
| 84 | **New 2026-09-05, same review, and it is a documentation defect with teeth.** `CLAUDE.md` and `docs/smp.md` both cited an **`IrreversibleCommandsTest` that has never existed.** The rule it claimed to assert - three `/smp` commands confirmed, three deliberately not - is asserted, by `SmpCommandsTest#whatIsIrreversible`, under a name nothing pointed at | **closed** - both documents corrected. Six more of the same kind went with it: five module test totals that disagreed with the table above them, "five families" listing six, a blockquote sitting *inside* a Markdown table and ending it, `/settle` left out of a rename, and `docs/i18n.md` still calling `smp`'s message bundle "not yet" four days after it was written |
| 85 | **New 2026-09-05, same review, and the fix is a decision rather than a patch.** **`command_request` had no deletion path at all.** Every settled row kept `requested_by`, `discord_id`, `mc_uuid`, `arguments` and a rendered `result` for as long as the database existed - while V11's own comment calls a request "a message in flight". Volume was never the argument; a season is a few dozen admin commands | **closed** - the owner chose 30 days on 2026-09-05. The updater deletes settled rows past the window once at the start of `serve`, next to `settleOrphans` and **not** on a timer, because that process is deliberately not a scheduler. A `PENDING` or `RUNNING` row is never touched however old it looks |
| - | **One finding was checked and rejected.** The review held that `PostgresNotifications#awaitNotification` can fail when the poll interval exceeds `socketTimeout`, leaving the listener reconnecting through every quiet period. It cannot, on the driver this build pins: `QueryExecutorImpl#processNotifies` in pgjdbc 42.7.13 saves the socket timeout, sets it to the requested notification timeout, catches `SocketTimeoutException` as "no notifications" and restores the original in a `finally` - read out of the shipped bytecode on 2026-09-05, not from memory. Left alone |
| 86 | **New 2026-09-05, from CodeRabbit's second pass, and it is a regression finding 74 introduced.** The generic `CHOICE` check compared **case-sensitively**, so `/phase set maintenance` was refused - a spelling that has worked in chat since the proxy's hand-written adapter, because `SetPhase#parse` compares phase names case-insensitively. Discord only ever sends the declared form, so the two surfaces would have disagreed about the one command an admin runs while the network is already misbehaving | **closed** - `Argument#match` is the single comparison, `Values` normalises a recognised choice to its declared spelling on the way in, and `RequestArguments#decode` hands the far side the declared form too. A value that is no choice at all is left exactly as typed, because the refusal quotes it back |
| 87 | **New 2026-09-05, same pass, and also from finding 77's fix.** `/phase show` chose between its two failure sentences on `held.isPresent()` - whether the process had a phase *cached*. On the path with no cache the phase line is produced **inside the same `try`** as the dates, so `currentPhase()` could succeed and `launch()` fail, and the answer then claimed there was nothing above it when there was | **closed** - it tracks whether the line actually went out, which is the question it was always asking |
| 88 | **New 2026-09-05, same pass, and from finding 81's fix.** `AccessRoles#member` answered *empty* when the configured guild was not available to JDA, and empty means "no such member" - so a guild the bot cannot currently see would have told an admin the person had left | **closed** - that branch throws, and the command's existing failure path answers `access.failed` |
| 89 | **New 2026-09-05, same pass, and from finding 83's fix.** `SmpPlugin.track` is written by `/smp reload` on Bukkit's async executor and read by the suggestion supplier on the server thread, with no happens-before edge between them - so the fix for the captured-instance bug could go on showing the old track for no bounded length of time, which looks identical to the bug it replaced | **closed** - the field is `volatile` |
| 90 | **New 2026-09-05, from CodeRabbit's third pass, and this one is older than the whole pull request.** `/smp reload` replaced `SmpPlugin.track` and reported success, while **four consumers went on reading the instance they were handed at enable** - `ObjectiveEngine` (what an objective pays), `StatisticPoller` (when one is reached), `NpcListener` and `BalloonListener`. So the one thing `milestones.yml` is a separate reloadable file *for* - appending a milestone or lowering a target mid-season - reached the suggestions, the season state and nothing that acts on them. A reload that says "the milestone track was reloaded: 12 milestones" and changes none of the arithmetic is the worst shape this can take | **closed** - all four take a `Supplier<MilestoneTrack>`, so the parameter type stops the instance being passed at all, and `ReloadReachesTheTrackTest` catches the next step (capturing a local and handing on `() -> captured`) |
| 91 | **New 2026-09-05, from CodeRabbit's fourth pass, and both halves are consequences of finding 90's fix.** `ObjectiveEngine#unlockMilestone` read the track supplier **three times** in one transition, so a reload landing between two of them would write a successor from one file and build the `SeasonState` beside it from another - the row then names a milestone the running state does not hold as active, and progression stops. And `StatisticPoller`'s baselines are raw statistic values keyed on an objective *key*, so an objective that starts counting coal **and iron** reads far higher on the next poll and credits the whole difference as progress somebody just made | **closed** - one snapshot per unlock, and the baselines are dropped when the track instance changes, which costs the one tick the first read of a session already costs |
| 93 | **New 2026-09-05, the first finding a real client produced *on the local stack*, and it explains three black screens seen on production that morning.** An admin joining during `PRE_LAUNCH` or `MAINTENANCE` was answered `STAY` at login - "leave their initial server alone", on the theory that they were never put in the waiting room and so were the one player not offered the pack. `STAY` leaves the choice to Velocity, and `velocity.toml`'s `try` list is `limbo`. So the admin landed in the waiting room after all, was offered the pack, applied it - and was then "released" onto the phase's backend, which `PhaseServers#forPhase` names as `limbo` for exactly those two phases. The station logged *leaving the waiting room for 'limbo'*, connected the player to the server they were standing on, and the visit ended: no timeout (the wait was over), no title change (nothing re-asked), no log line that read as wrong. Every second join of the day on the production proxy sat there for ever; `todo.md` had two hypotheses and neither was it | **closed the same day, twice over.** Everybody goes through the room now, admins included; `PhaseServers#forAdmitted` releases an admin in those two phases onto the SMP (fixed, no config key - owner's decision), `PhaseRouting#decideRelease` is the release half so that `decideAdmitted`'s `STAY` keeps meaning "an admin already on a backend is not moved on a phase change", and `LimboHold` learned the admin flag so that maintenance holds everybody *but* the person doing it. Reproduced, fixed and re-verified on the local stack with a real client: three consecutive joins released onto `smp` within eight seconds. The second half was found on the way: **`limbo`'s single READY was lost on two of three logins** (finding 38's second path, the read batch), and the five-second grace period caught each one - so `limbo` now repeats READY every second until the player is gone, the book treats a repeat as ordinary rather than "early", and every release since has been confirmed and immediate |
| 94 | **New 2026-09-05, from the local stack with a real client.** `PackStation` carries a `DECLINED` branch that disconnects the player with `pack.declined` in their language, and `todo.md` asked whether that text wins the race against Velocity's generic forced-pack kick. Neither text is ever seen: on a **forced** offer the client disconnects *itself* the moment the player presses Decline, with its own line (*This server requires a custom resource pack*), and the proxy sees a plain disconnect - no status packet, no log line, nothing to race | **open by decision, documented rather than fixed.** The branch stays, because a non-forced offer (`pack.yml#force: false`) would reach it, and the player is not stranded either way - which is the half that matters. `pack.declined` is translated text nobody will read while the pack is forced, and this row is what says so |
| 95 | **New 2026-09-05, from the local stack, and it is a decision reversed by seeing it.** `/phase set SMP`, typed twice on the proxy by the admin, worked - and its re-route disconnected that admin with *no active access*, because `AccessState#mayJoin` and `GateOutcome` both said, in tests with names like `theAdminFlagIsNotAFreeAccessPeriod`, that the flag decides *where* a player goes and never *whether*. Correct as written since 2026-08-31, and the person opening the season would have been the first one thrown out of it | **closed the same day** by the owner's decision: the admin flag is an access period in every phase, the way it already exempts an admin from the player limit. One line each in `AccessState` and `GateOutcome`; five tests rewritten to say the opposite with the reason; a banned admin is still banned, because member state is asked first |
| 96 | **New 2026-09-05, from the local stack, and it is finding 64 a third time.** `SmpPlugin` built a `PlayerLocales`, handed it to fifteen classes, and **nothing in `smp` ever called `joinAsync`** - so `of()` answered English for every player on the SMP for the whole session. `CLAUDE.md` said the module "inherits this rule rather than rediscovering it"; `limbo` and `hunger-games` each call it in their `PresenceListener`, `smp`'s did not. Found by the first German account reading `/smp` in English a minute after the proxy had answered `/phase` in German: both modules were internally consistent, and the language was wrong only at the seam | **closed the same day.** `smp`'s `PresenceListener` calls `joinAsync` at join and redraws the tab-list frame when the value lands, `quit` at quit; the HUD and the boards render from `of()` on their own timers and heal themselves. `LocaleJoinWiringTest` in `:common` asserts all three backends do both - the same shape as `AdminWatchWiringTest`, for the same class of failure: a mechanism that exists, is tested, and is called by nobody |
| 97 | **New 2026-09-05, from the local stack, and it is the most expensive one of the day.** `SmpDao#completeMilestone` wrote `state = 'COMPLETE'`; V6's `smp_milestone_state_check` allows `LOCKED`, `ACTIVE`, `UNLOCKED`, and `MilestoneState` agrees with the schema. So **every milestone unlock threw** - the escape hatch `/smp milestone unlock` answered "the database did not answer, nothing was changed", and the automatic unlock at the end of a milestone's objectives would have died on the same statement, in the season's third week, with the chat announcement, the border expansion and the payout all behind it. `completedMilestoneKeys()` read `'COMPLETE'` back, so the list of unlocked milestones was empty by construction and nothing ever disagreed. 176 tests in `smp`, none of them against the constraint | **closed the same day** - both statements say `UNLOCKED`, and `MilestoneStateIntegrationTest` (3) walks LOCKED → ACTIVE → UNLOCKED against the real constraint, the idempotent second completion, and the hand unlock of a milestone that was never active. Found by typing the command on a real server; a value that only meets a constraint in a database cannot be found anywhere else |
| 98 | **New 2026-09-05, from the local stack.** `/phase` with nothing after it printed the help - four usage lines - since the command fold, because a bare root lists what is under it. `todo.md` and the proxy's own history expected `/phase` to *be* `/phase show`: it is the command typed while the network is misbehaving, and what that person wants is the phase | **closed the same day** by the owner's choice: `Catalogue#rootDefault` names `/phase show` as the one bare-root default, both Brigadier adapters run it instead of listing, and `CatalogueTest` holds every such default to a two-segment path under its own root with nothing required after it. `/smp`, `/hg` and `/access` keep the help |
| 99 | **New 2026-09-06, from the local stack, and it is the largest thing the rehearsal found.** `smp_objective` had **no insert anywhere in the repository**. `ensureMilestone` ran for every milestone at enable; nothing did the same for an objective, so the table stayed empty for as long as the season ran: `objectivesOf` answered nothing, the objective board showed the milestone's name with no rows under it, the NPC's list was empty, and no hand-in, statistic or advancement could have booked a single unit - the whole progression, from the second milestone on, could not have happened. 187 tests in `smp`, none of them against the table | **closed the same day.** `SmpDao#ensureObjective` (insert, idempotent, target and type updated on conflict so the first escape hatch reaches the row), called from `ensureRows` at enable and after every accepted `/smp reload`. `MilestoneStateIntegrationTest` drives it against the real constraints. Found by *looking at the board* after unlocking a milestone with objectives - the third milestone; the first two have none, which is why nothing before this day could have noticed |
| 100 | **New 2026-09-06, from the local stack.** The spawn NPC was `setPersistent(false)`: Paper discards such an entity when its chunk unloads, and on the local stack - placeholder coordinates far from the world spawn - it was gone within minutes, leaving the spot empty until the next restart. On Nordtal the spawn chunks would usually keep it alive, which is the kind of "usually" a season is long enough to fall through | **closed the same day** - persistent, and `sweep()` at spawn already removes what the previous start left, so a restart makes no duplicates |
| 101 | **New 2026-09-06, from the local stack, and it is a production shape.** `deploy/dev deploy smp` replaces the plugin's jar under the running server and then restarts it; the restart's `onDisable` died on its fourth step with `NoClassDefFoundError: Duels$ActiveDuel` - an inner class the JVM had never needed until then, read from a file that no longer existed - and **every step after it was skipped**: the grave and board displays stayed in the world, the poller, the admin watcher and the pool were never closed. `updater apply` does the same to a production server, jar first and restart second, so this is not a development-only failure | **closed the same day.** `Shutdown#quietly` in `:common` runs one step and logs a failure - `LinkageError` included, which a plain `catch (Exception)` lets through and which is exactly the error a replaced jar produces - and all 26 disable steps across the three Paper plugins go through it. `DisableStepsAreIsolatedTest` is a text search that fails on the next bare `x.stop();` in an `onDisable` |
| 92 | **New 2026-09-05, found while chasing 91 rather than reported.** **`TrackValidation.validate(track, progress)` was called by no line of production code.** It answers "may this file replace the running one" - a renamed milestone key that orphans its rows, an objective that changed type with progress against it, a completed objective whose target moved after it paid out - and it had twelve tests, two javadoc references from `SmpDao` and `SeasonState`, and no caller. So `/smp reload` applied any file that parsed, reported success, and could remove the active milestone and stop the season with nothing anywhere saying why. It is the same shape as `AdminOperators#refresh` (finding 47): a mechanism that exists, is tested, and does nothing, on a question where doing nothing looks identical to working | **closed** - the owner chose **refuse and keep the running track** on 2026-09-05. `SmpDao` gained the two queries the check needs, the reload asks it before the swap, and the answer names each disagreement rather than saying something went wrong - an operator editing `milestones.yml` mid-season is the one person who can act on "you renamed a key that has rows against it". The sounds and the wording are still re-read regardless, because the three fail independently |
| 102 | **New 2026-09-06, from the CodeRabbit review of PR #8, and it is the one with a security shape.** A bare `/phase` runs `/phase show`, which finding 98 added the day before - and both Brigadier adapters reached it by calling the child's `run`/`dispatch` **directly from the root handler**, which goes around the child node's `requires`. That `requires` is the entire admin gate for a command tree: neither adapter's dispatch has ever carried an admin check of its own. So any player on the proxy could type `/phase` and be told the phase, the launch date and the SMP start date. Both files carried a comment stating the opposite - *"the child's own dispatch applies the admin check, so nothing is skipped"* - which is the reason it survived review by two people | **closed the same day.** The question moved into `Catalogue#rootDefault(root, admin)`, the one place that knows a root has a default at all; both adapters pass the same `mayUse` Brigadier would have consulted, and a refused default falls through to the help path, which already hides admin-only commands. `CatalogueTest#theRootDefaultIsGated` asserts it for every root that has a default, not just for `/phase` |
| 103 | **New 2026-09-06, from the review.** `/smp reload` assigned `track = candidate` and *then* called `ensureRows(candidate)`. If that throws halfway - it writes one row per milestone and one per objective - the catch below reports that the running track is unchanged, which was a lie: every command was already reading a definition whose objective rows were missing or half written, and finding 99 is what that state costs | **closed the same day** - rows first, assignment second. The failure now means what the message says |
| 104 | **New 2026-09-06, from the review.** `/smp status` read `Bukkit.getOnlinePlayers().size()` from the effects' executor - an async task on the chat surface, the request thread from the inbox - and Paper documents that collection as main-thread only | **closed the same day.** A `volatile int` sampled by a one-second main-thread task; the status line takes a number that is at most a second old, which is what a status line needs |
| 105 | **New 2026-09-06, from the review.** The locale callback added by finding 96 called `locales.quit(uuid)` when its captured `Player` was offline. A player who leaves and rejoins inside the query's flight time has a *newer* session under the same UUID, and the cache is keyed by UUID - so the old callback deleted the language the new session had just loaded, leaving that player English for the rest of it | **closed the same day** - `quit` only when no player is online under that UUID |
| 106 | **New 2026-09-06, from the review, and it is finding 100's other half.** `sweep(world)` uses `getNearbyEntitiesByType`, which searches **loaded chunks only**. Making the spawn NPC persistent means it is now saved to disk - so on a restart the sweep finds nothing in an unloaded chunk and the spawn adds a second figure on top of the first, which is precisely the duplicate the persistence comment said the sweep prevented | **closed the same day** - the configured chunk is loaded before the sweep runs |
| 107 | **New 2026-09-06, from the review.** The ten-second retry window added by finding 95 was recorded against the *player*, with no destination on it. A phase change inside those ten seconds points at a different backend, and `PlayerRouter#rerouteOne` re-evaluates every held player on one - so the new backend inherited a failure that was never its own and the player stayed on the `BACKEND` screen | **closed the same day** - the window carries the destination that refused, and `WaitingBookTest` gained the phase-change case |
| 108 | **New 2026-09-06, from the review.** `currentGameIdNow()` refreshed the cache and then returned the *field*, off the main thread. `onGameDecided` clears that field on the main thread, so a decision landing between the query and the return made the method answer "there is no hunger games event registered" although its own query had just found one | **closed the same day** - it answers with the local value, refuses to write back a game that has already been decided, and still refreshes the cache on the way past, because it is the only thing that refreshes it after enable |
| 109 | **New 2026-09-06, from the review.** `Announcements#post` returned `true` after `RestAction#queue`, which returns before the request is sent. So `announce` settled its `command_request` row as posted, and the failure callback - a missing channel, a permission, a rate limit - ran long afterwards into a log nobody was reading | **closed the same day** - the post is waited for with a 15-second bound and the boolean means Discord took it. A timeout counts as not posted, deliberately: an announcement reported as failed and delivered late is a puzzle, one reported as posted and never delivered is a silence nobody investigates |
| 110 | **New 2026-09-06, from the review.** `Applier` moved staged jars with `REPLACE_EXISTING` alone. Across a device boundary `Files.move` silently degrades to copy-and-delete, which is exactly the half-written jar in `plugins/` the two-phase swap exists to prevent - and it would degrade in silence, on a host nobody is watching. The staging directory was moved *inside* its destination on 2026-09-05 for this very reason, and the flag that would have proved it was never asked for | **closed the same day** - `ATOMIC_MOVE`, and an `AtomicMoveNotSupportedException` is an `IOException`, so it lands in the existing failure path that says the server may be part-updated |
| 111 | **New 2026-09-06, from the review.** `smp.announce.milestone` read *"{milestone} is complete - the border grows"* and was sent for **every** milestone. The border only moves when the unlock is `BORDER`, so a Nether or End milestone posted a growth that did not happen - into Discord, the one surface where nobody can check it against the world | **closed the same day** - four sentences selected by the unlock, in both languages, with the neutral one kept for `NOTHING` |
| 112 | **New 2026-09-06, from the review.** `deploy/dev` created and copied the pack into a hardcoded `deploy/pack` while `compose.yml` serves `${PACK_ROOT:-./deploy/pack}`. With `PACK_ROOT` pointed anywhere else, `deploy/dev pack` wrote a correct URL and a correct SHA-1 into `pack.yml` for an archive the host does not serve, and every client then failed the download | **closed the same day** - a `pack_root()` beside `servers_root()`, read from the env file for the same reason |
| 113 | **New 2026-09-06, from the review, and it is the one that could have deleted a world.** `deploy/dev reset` found the volume by `label=com.docker.compose.volume=mc-<service>` alone. A single volume of that logical name belonging to **another compose project** on the machine passes the "exactly one" check the comment there is so careful about, and is removed. The project is `nordtal-s2` and the directory is `season-2`, so guessing the label was not an option either | **closed the same day.** The volume is read off the container's `/data` mount while the container still exists, which is exact; only if there is no container does it fall back to the labels, and then on the project label too, resolved from `COMPOSE_PROJECT_NAME`, then `compose.yml`'s own `name:`, then the directory |
| 114 | **New 2026-09-06, from the review.** `ARCANE_URL` is `http://host.docker.internal:<port>` in the local runbook and `ARCANE_API_KEY` travels on every redeploy as an `X-Api-Key` header, so the key is in the clear | **closed as a warning, not a refusal.** Locally the request goes container-to-host over Docker's own bridge and leaves no machine, and the updater is the one process that migrates - refusing to start over an optional restart button would trade a working schema for it, which is the reasoning the loopback check beside it already carries. `Arcane#cleartextWithKey` names it at startup, `.env.example` and `deploy/README.md` say production is `https://`, and `ArcaneDiagnosisTest` pins the four cases |
| 115 | **New 2026-09-06, on the local stack, on the first deploy that carried the fix for finding 101 - so the guard was undone by the very shape it guards against.** `Shutdown#quietly` lives in `:common`, shaded into the plugin jar, and a class is loaded on **first active use**. Its only use was inside `onDisable`, so the first `quietly(...)` of the first restart after a jar swap threw `ClassNotFoundException: eu.nordtal.s2.common.health.Shutdown` and the disable sequence died at step **one** - worse than the four-steps-in it was written to fix, and it would have shipped that way | **closed the same day.** `Shutdown#warmUp()` is called as the first line of all three `onEnable`s, which loads and links the class while the jar still exists. Verified on the running local SMP: the swap before the fix printed `NoClassDefFoundError` again, the swap after it printed `smp disabled` with the pool closed and no error at all. `DisableStepsAreIsolatedTest` gained the case. It warms that one class and nothing else, deliberately - a step whose own classes have gone is exactly what `quietly` absorbs |
| 116 | **New 2026-09-06, on the local stack, and it is the price of finding 96's fix rather than a miss.** The language is a database read taken off the main thread, so `of()` answers English until it lands. Every surface on the SMP is redrawn on a timer and picks the language up by itself - except the join line, which has exactly one moment, and that moment is before the query returns. A German player therefore read *"hmtill joined."* underneath a German HUD and a German objective board, on the very join that had just loaded German | **closed the same day.** `SystemLines#onJoin` now only suppresses the vanilla line; the replacement is `announceJoin`, called from `PresenceListener`'s locale callback (`whenComplete`, so a failed load still prints it in English rather than swallowing it). Verified on the running local SMP: *hmtill ist da.* `LocaleJoinWiringTest` gained the case, including the half that matters most - that something still calls it, because a join line nobody sends looks like a quiet server |
| 117 | **New 2026-09-06, on the local stack, and it undid a decision taken the day before.** Both Brigadier adapters put `requires(mayUse)` on **every** child of a root, whatever the child declared. `/smp status` was made the one `/smp` command a player may run on 2026-09-05 - `CatalogueTest` names it and `announce` as the only two declarations that are not admin-only - and a player who typed it got Brigadier's `Incorrect argument for command` with a red caret under `status`, because the node had been hidden from their tree. The declaration said one thing and every adapter did another, and no test could see it: the flag was read nowhere | **closed the same day.** A subtree carries the check only when everything runnable at or below it is admin-only, which also keeps `/hg ready`'s shape working for any future open command; the admin-only nodes deeper down still carry their own. Verified on the running local SMP - a non-admin now reads *The network is in SMP. Current milestone: Foothold, 0 % done. 2 on the SMP*, which also confirms finding 104's main-thread player count. `AdminSourceTest` gained the case, plus the half that keeps it honest: it fails if nothing in the catalogue is open under a root any more |
| 118 | **New 2026-09-06, from the first two-player duel on the local stack.** `DuelListener` listened for `PlayerMoveEvent` alone. `PlayerTeleportEvent` extends it but declares its own handler list, so a move handler never sees a teleport - and **every way of travelling on this server is a teleport**: the balloon, a respawn, `/tp`. A player teleported onto a platform was not registered as waiting, and, the half that matters, a player teleported *away* from one stayed registered, so the next arrival would have been put in an arena against somebody who had taken the balloon elsewhere. The same walk also printed *Warte, bis jemand auf der anderen Seite steht.* three times, once per block crossed inside a 3x3 | **closed the same day** - `onTeleport` delegates to the same body, and the line is only sent when the player was not already the one waiting there |
| 119 | **New 2026-09-06, and the duel could never have worked in production.** `steppedOn` runs inside `PlayerMoveEvent`, and Bukkit applies `event.getTo()` to the player **after** the handlers return - so a teleport performed inside the event is silently undone, for that player only. The fighter who was already waiting was moved into the arena at y=201; the fighter who had just stepped on stayed on the platform at y=68, in ADVENTURE mode, holding the free loadout, in a duel that ran and was scored. `enter()`'s own guard passed, because `Player#teleport` returned `true` | **closed the same day** - the duel begins on the next tick. Verified on the running local SMP: both fighters at y=201, at opposite ends of the arena. `DuelStartIsDeferredTest` is a text search, because the failure needs two real players and there is no Bukkit in a unit test |
| 120 | **New 2026-09-06, found by reading the aura table after the first duel that worked.** A duel death booked `DUEL_LOSS -10` **and** `DEATH -5`, seven milliseconds apart. `GraveListener` asks *is this player in an arena?* at `HIGH` precisely to skip the grave and the penalty - and `DuelListener` runs at `LOWEST` and ended the duel there, so by the time the question was asked the answer was no. docs/README.md states it in one line: deaths cost aura, **nothing in the duel arena**, and a duel only ever moves ±10 between two players | **closed the same day** - the duel is decided on the next tick, so arena membership survives the whole event. Verified against the table: the next duel booked +10 and -10 and nothing else |
| 121 | **New 2026-09-06, and it is finding 118's own fix biting back.** Restoring a fighter is a teleport to where they stood when the duel began - which is the platform. Once a teleport counted as a move, the restore re-registered both of them, and the second duel started in the same tick the first ended: two people who fought once fought for ever, having touched no key. Seen as both fighters still at y=201 twenty seconds after a duel had been settled | **closed the same day** - a restored fighter is `settled` until they physically step off the platform, which is the act the rule was always about. Verified: after a duel both stand on the platform and nothing happens |
| 122 | **New 2026-09-06, and it is the one the rehearsal checklist told us to watch for.** `todo.md` says of the duel restore: *if it ever loses an item, stop using duels until it is found - `SavedState` is the only thing standing between a duel and somebody's whole inventory.* It lost all of them. The loser of a duel is **dead** when the duel is settled, and an inventory written onto a dead player is discarded by the respawn, which hands back whatever they died holding - the arena's loadout. So every loser walked away with a free iron sword, a shield and eight steaks, and their own inventory was gone. Measured: thirteen emeralds in, an iron sword out. Nothing logged anything, and `SavedState.restore` had done exactly what it was told | **closed the same day.** A dead fighter's state waits in `pending` until `PlayerRespawnEvent`; the event's respawn location is redirected to where they stood before the duel, and the inventory goes on one tick later, because the respawn writes the player's contents after the event returns. Verified on the running local SMP: thirteen emeralds in, thirteen emeralds out, respawned on the platform. `DuelStartIsDeferredTest` gained the case |
| 123 | **New 2026-09-06, read off a real farm-reset warning.** *The farm world resets in 1 minutes.* The four warnings are 30, 10, 5 and **1** minutes, so the singular is not an edge case - it is the last sentence every player reads before every daily reset, in both languages, in chat and in Discord | **closed the same day** - `smp.farm.warning.one` and `smp.announce.farm-reset.one`, chosen when `minutes == 1`. A second key rather than plural machinery: two languages, one number, one sentence each |
| 124 | **New 2026-09-06, twice over, while building the duel ending the owner asked for.** A duel now ends with both fighters **at the spawn** - and `world.getSpawnLocation()` is a coordinate, not a promise. The first version teleported both into solid rock and suffocated them; the safe-column search written to fix that answered *the bedrock at y=-63* for a column it could not read, and dropped both out of the world with *left the confines of this world*; the version after that accepted a spawn sitting **in lava**, because a liquid is passable and the check asked the loose question. Three deaths in a row, each one second after a duel that had just been settled as costing nothing | **closed the same day.** `LandingSite#safeAt` takes the preferred spot when a player actually fits there - two **air** blocks over ground the same search already calls good - and searches outwards from that column otherwise; a column whose highest block is within four of the world floor is refused outright. Verified on the running local SMP with the world spawn moved to the surface: both fighters at the spawn, 20 health, thirteen emeralds each still theirs |
| 125 | **New 2026-09-06, on the first farm-world reset anybody has ever run, and it would have cost the season its farm world.** `FarmWorldSwap#folderOf` falls back to building a path when the world is not loaded - and **the unloaded case is the ordinary one**, because the swap unloads the staged world a moment before it looks for its folder. The fallback built it from `primary.getWorldFolder()`, which on Paper 26.2 already ends in `dimensions/minecraft/overworld`, so the answer was `nordtal/dimensions/minecraft/overworld/dimensions/minecraft/farm-next`. That matches nothing: every daily reset would have logged *there is no staged farm world … reset aborted, today's world is untouched* at five in the morning and left yesterday's world in place, for ever. Finding 18 had already written down that a Bukkit world lands inside the primary world's folder; this is that same fact applied twice | **closed the same day.** The path is built from `Bukkit.getWorldContainer()` and the level name, and it is a static function with a unit test - the version that could not be asserted was wrong. Verified on the running local SMP: *the farm world was replaced; the new arrival point is -60/63/40*, and the retired world deleted asynchronously |
| 126 | **New 2026-09-06, from the same reset.** `Bukkit.createWorld` for the next staging world runs on the server thread and took **15 seconds**, long enough for Paper's watchdog to dump the stack twice. It is unavoidable in the API - there is no asynchronous world creation - and at 05:00 with nobody online it costs nothing | **closed 2026-09-07 by the owner's choice: delay it.** `FarmWorldReset.STAGING_DELAY` is one minute; the swap and the teleport finish first, the freeze lands when nobody is waiting on anything. `stop()` was split in two on the way, because `scheduleNext()` cancelled every task and would have removed the delayed build one line after planning it. `StagingIsDelayedTest` (4) is red on the old shape |
| 127 | **New 2026-09-06, and only a client could have found it.** The spawn NPC stood in the tavern with **no label at all**. `config.yml#npc.name` defaults to `Nordtal`, its comment reads *The label above it*, `data get entity` reported `description: "Nordtal"`, and the plugin logged nothing - every check that does not involve looking at the figure agreed with the code. What `Mannequin#setDescription` actually is, measured the same day by renaming the live entity and then removing each field in turn, is the **lower, smaller** of two lines; the upper is the ordinary entity custom name, which was never set. Its default is the literal English word `NPC`, so leaving it alone labels the figure a second time and in one language for every reader | **closed the same day.** The configured name is the entity's custom name, made visible, and the description is explicitly emptied - exactly one line, verified on the client. `SpawnNpcLabelTest` asserts both halves. **What was not established** is why the figure standing there that afternoon drew neither line while carrying the description in its NBT; the fix does not depend on the answer, which is why it was not chased further |
| 128 | **New 2026-09-06, read off the wheel refusing a second spin.** *No spins left today. You have 0 earned spin(s) waiting.* A parenthetical plural is not a sentence, and the German it was translated into is worse - `verdiente Dreh(s)`, and in `:commands` `Zugangszeitraum/-räume von {accounts} Konto/Konten`. Six values across three places; `RevokeAccess`'s own javadoc already quoted the shape it was working around. The zero case was the loudest: the refusal says there are none and the next clause counts them anyway | **closed the same day** - a count picks a key, the way finding 123's farm warning already does. `BundleContinuationTest` gained an absolute rule against `(s)`/`(n)`/`(en)` in any bundle value. **It also gained `commands/src/main/resources/messages` as a root**, which it had never carried: the shared bundle every surface loads was the one bundle outside the continuation check, and it carries continued lines |
| 129 | **New 2026-09-06, and it is the one thing `milestones.yml` is a reloadable file for.** Its header promises that lowering a target below the collected progress makes the objective "complete at once and pay its FULL pot"; `TrackValidation` permits the lowering and has a test for permitting it, and `ObjectiveEngine#payOut` names the case in its own javadoc. **Nothing did it.** Completion is decided inside `credit`, when progress is *added* - so a lowered target took effect the next time somebody handed something in, and for an objective that has become impossible there is no next time. That is precisely the case the escape hatch exists for. Measured on the local SMP: `diamonds` at 20 with its target moved to 10, `/smp reload` reported success, and the row sat unfinished with no aura paid | **closed the same day.** The reload finishes every objective its new targets already reach, after `ensureRows` and after the track is applied - `finishObjective` guards itself in SQL, so a reload that changed nothing does nothing. Verified: *objective 'diamonds' is already at 20 of its new target 10 - completing it now*, the full 60-aura pot paid to the one contributor, `-186` to `-126` |
| 130 | **New 2026-09-06, from the first Nether portal anybody has lit here.** The refusal works - *the Nether is not open yet* - and then charges the player for it. Cancelling `PortalCreateEvent` stops the portal and nothing else: the flint and steel has already placed a fire block, and vanilla leaves it because to vanilla the frame was merely invalid. Here the frame is valid and the server said no, so the player stood in the flames losing hearts while reading the refusal, and a death there would have cost five aura on top | **closed the same day** - the blocks that would have become portal are swept for fire on the next tick, which is where the ignition's own call stack has finished. Verified: the message arrives, no fire block remains anywhere in the frame, 20.0f health. `RefusedPortalLeavesNothingTest` pins the call and its tick |
| 131 | **New 2026-09-06, and it is a rule this repository wrote down and never implemented.** `docs/smp.md`: *every portal in the farm world leads to the Nordtal spawn, wherever it stands* - the farm world is thrown away daily and must not become a permanent address. `PortalGate#onPortal` has the branch that does it. **The branch is unreachable.** The farm world is a custom dimension (`minecraft:farm`), and vanilla's portal travel links `minecraft:overworld` to `minecraft:the_nether` and nothing else, so the server never attempts a transfer and never raises `PlayerPortalEvent`. Measured: a frame in the farm world lights (correctly - the milestone gate does not apply there), the portal forms, the screen goes purple, and the player stands in it indefinitely. The identical frame in Nordtal moved them to 30/70/27 in the Nether and back to the exact block, both within seconds | **closed the same day** - the exit hangs off `EntityPortalEnterEvent`, which is raised from the block the player stands in rather than from the travel logic, and so arrives in any dimension. Four seconds, vanilla's own dwell time, armed once rather than per tick, re-checked before it fires, and landing through `LandingSite#safeAt` for finding 124's reason. Verified: standing in the farm portal now arrives at the Nordtal spawn. The dead branch is kept, and says why |
| 132 | **New 2026-09-06, and nothing in the game could have shown it.** `smp_grave.looted_by` is `varchar(32)`, the same shape as `owner_id` beside it, because every person in this schema is a discord id. `Graves#onClosed` passed the looter's **Minecraft UUID**, whose 36 characters do not fit, so `markGraveLooted` threw *value too long for type character varying(32)* on **every single loot** - and it threw from inside the async task that also erases the grave and refunds the experience, so none of that happened either. **No grave has ever been marked looted**: eleven were being restored on every start of the local server, the markers accumulated, and nobody ever got their levels back. What a player can check is that the window opens, the items come out and the window closes, and all three were true throughout | **closed the same day** - the looter's discord id through `Identities#discordIdOf`, null when they are unlinked because the column is nullable for exactly that. Verified: `looted_by = 100000000000000002`. `GraveLootIntegrationTest` drives the statement against the real schema and pins the width as the *reason*; `PeopleAreDiscordIdsTest` makes it an absolute rule across the six modules, written while there were zero call sites left |
| 133 | **New 2026-09-06, found by re-opening the grave from finding 132 after a restart.** It held seventeen emeralds and three golden apples again - and the player who had emptied it still had theirs. **A loot was never written back to the row.** `onClosed` kept what was left in the plugin's own map and nowhere else, so the enable-time restore, which reads `smp_grave.contents`, refilled the grave from the original blob. Finding 132 made every grave qualify for that restore; but the duplication is a defect of its own, and survives its fix: half-empty a grave, log off, let the server restart, and it is full again | **closed the same day** - `updateGraveContents` writes the remainder back, guarded by `looted IS NULL` so a grave somebody else finished is not refilled by a late close. `GraveLootIntegrationTest` covers both halves |
| 134 | **New 2026-09-06, on the first balloon anybody has taken to the Nether.** `BalloonGui` teleports to `destination.getSpawnLocation()` raw, and its comment explains why - *always the world spawn, which is what makes a world spawn a landmark everybody knows*. The design is right and the coordinate is not a promise: the Nether's world spawn is 0/66/0, which on a generated Nether is solid netherrack, so the trip ended in `DEATH_LISTED -20`, `in_wall`. **This is the third place in one day**, after the duel ending (124) and the farm world's portal exit (131), and two more raw uses were still standing when it was found - the farm-world reset, which is the one teleport here that moves *everybody* at five in the morning, and the portal event's farm branch | **closed the same day.** All five arrivals go through `LandingSite#safeAt`, which takes the preferred spot whenever a player actually fits there - so a built world keeps its landmark exactly and only the case that used to be a death changes. Verified: 0.5/128/0.5 in the Nether, 20 health. `LandingIsCheckedTest` makes it a rule over `smp`'s sources, with one named allowlist entry that moves nobody. **What it does not fix** is that a generated Nether has no floor at its spawn column, so the search lands on the roof - written into [`todo.md`](../../todo.md): the Nether's and the End's world spawn have to be set where their balloon platforms are built |
| 135 | **New 2026-09-06, and it is a finding about a comment rather than about behaviour.** The farm-world reset warns by name about anybody it could not move out, and the review of 2026-09-04 that added it listed *dead* first among the reasons - the farm world being *exactly where people mine and die*. A dead player is **not in `World#getPlayers()`** until they respawn, so the evacuation never sees them: no warning, `Bukkit.unloadWorld` succeeds because Bukkit agrees the world is empty, and the swap goes through. `todo.md` asked for a rehearsal that produced a console warning naming the player; that warning cannot happen | **closed the same day, by correcting the claim rather than the code.** Measured: somebody dead in the farm world through a reset respawns at their spawn point, alive, with nothing said anywhere - which is a better outcome than refusing the nightly reset because one person is on a death screen. The guard still covers what it can (asleep, mid-disconnect, carrying a passenger); the comment now says which case it does not, and `todo.md` records what actually happens |
| 136 | **New 2026-09-06, from the rehearsal step that asks what happens if somebody logs off mid-spin.** The answer to *that* question is good: a kick one second into the animation paid sixteen oak saplings, because Paper turns a disconnect into an `InventoryCloseEvent` and the close handler finishes the wheel. **A server shutdown is a different path and lost the spin.** Paper disables plugins *before* it saves and disconnects players - `Disabling smp` and the pool's `Shutdown completed` both come several lines above `annicx lost connection` - so the close event arrives with no listener registered, the animation's own chain stops at its next tick, and the spin that was already spent in SQL bought nothing | **closed the same day.** A disable step hands over any prize whose animation is still running, before anything else that could throw and long before the pool: the players are still online there, so it gives the item rather than needing the refund, and `Saving players` three lines later is what writes it to disk. Verified: 128 stone bricks in the inventory after the server was restarted one second into a spin. `SpinSurvivesShutdownTest` pins the step and its position |

| 137 | **New 2026-09-07, from the two-client rehearsal, and it is the one rule `docs/smp.md` states in a single sentence: *Disconnecting is a defeat and the aura is booked. Otherwise logging out is a free escape from losing.* It was not booked.** `Duels#book` resolved both fighters' discord ids out of `Identities`, a per-session cache that `JoinGate`'s quit handler clears - and `JoinGate` is registered at `SmpPlugin:362`, `DuelListener` at `408`, both at `NORMAL`, so Bukkit runs them in that order. By the time the duel's own quit handler reached `book`, the leaver's id was gone and the method took its early return, writing **nothing at all**: not the loser's stake and not the winner's. Everything visible still happened - the arena came down, the survivor was returned to the spawn with their own inventory and a "you won" title - and only the number was missing. Measured: a mid-fight disconnect left 50 / -142 untouched and `smp_aura_event` with no row | **closed the same day.** `ActiveDuel` carries the two discord ids, captured where the duel is built and both fighters are certainly online. Reordering the two listeners would also have worked today and was rejected: it makes whether a duel pays out depend on a registration order nothing states and any later edit to `onEnable` can reverse. Verified after the redeploy: a quit mid-fight booked `DUEL_WIN +10` / `DUEL_LOSS -10` (50 -> 60, -142 -> -152) and returned the survivor to the spawn. `DuelStakeSurvivesAQuitTest` is red on the shipped version - checked by putting it back |
| 138 | **New 2026-09-07, from the first `/hg start` anybody has run with participants on the server.** Both participants were kicked within ten seconds - *was kicked for floating too long* / *Flying is not enabled on this server* - and the kick was not the end of it: the proxy put them back, the start placed them on the same tower, and five seconds later it happened again. An unbreakable loop, for every participant at once, at the opening of the season's flagship event, with no message anywhere naming a cause other than vanilla's. `FreezeListener` cancels every position change for the whole countdown, so a participant above air does not fall - they hover, which is exactly what vanilla's check punishes. Locally there is nothing under the towers because the arena world is in no repository; on the real arena the towers exist, but the freeze is what turns *one tower's top block is missing, or one lower than `spawn-tower-height`* into this | **closed the same day.** `placeOnTower` grants mayfly - the flag the vanilla check actually reads - and `release` takes it back, landing the player first. It grants no movement, because the freeze cancels that. Building the towers from the plugin was the alternative and is a much larger decision: the arena is hand-built and this module places nothing. Verified after the redeploy: two full starts, countdown through release, **zero** kicks. `FrozenPlayersAreNotKickedTest` pins the grant, the revoke, its ordering, and the freeze that makes it necessary |
| 139 | **New 2026-09-07, on the same first real start, and visible on the client the whole time.** The hunger games HUD's first line read **Alive 0, Dead 0** with two live participants standing in the world, and its second read *no further refills planned*. Both were fed by setters - `HudRenderer#setCounts` and `#setNextRefillAt` - and **neither had a single caller anywhere in the repository**. Both halves of each wire existed and nothing joined them: `WinTracker#aliveCount()` and `#deadCount(int)` had no caller either. So for the whole of every game, the one screen every participant looks at showed a zero | **closed the same day, by deleting the setters.** The renderer reads `WinTracker` and a new `LootRefill#nextRefillAt()` on each redraw, from the objects the death handler and the schedule already write to - a push would work and would break again the same way, because *remember to call this* is a rule with no enforcement. Verified on a real client: **Alive 2 Dead 0** and **Next refill in 59:33**. `HudReadsItsNumbersTest` forbids the shape rather than the two instances - the renderer may have no `setX` at all |
| 140 | **New 2026-09-07, from the login-path rehearsal, and it is about a message key rather than a bug.** `todo.md`'s step 7 asks for `pack.invalid-url` by pointing the pack URL at something that is not a zip. That does not produce it: the client answers `FAILED_DOWNLOAD` for a 404 and for an HTML page alike, and a URL it genuinely could not load never gets past `Configs`, which requires `http://` or `https://`. So `PackStation`'s `INVALID_URL` branch has no path this rehearsal can reach - the same shape as finding 94, where `force: true` makes the `DECLINED` branch unreachable because the client disconnects itself first | **kept, not deleted.** A defensive branch for a status the protocol defines costs nothing, and the rehearsal step is the thing that was wrong: it now says so and asks only for the half that can be seen. **The half that can** was verified the same day - `FAILED_DOWNLOAD` with `pack.failed-download` verbatim - and it needed a hash the client had never seen, because a client that already holds a pack under that `sha1` never touches the URL at all. That last fact is the operationally interesting one: a broken pack URL is invisible to everybody who already has that exact version |
| 141 | **New 2026-09-07, from the maintenance rehearsal.** The proxy has re-read the admin roster on its poll and its `LISTEN` since 2026-09-02, and the reason written beside it is exactly right - *an emergency revocation is precisely the case where waiting for a disconnect is the wrong direction*. What that fixed was **authorisation**: who may run `/phase` and `/smp`. It moved nobody. In `MAINTENANCE` the admin flag is the entire difference between standing on the SMP and being held in the waiting room, so an admin whose rank was revoked mid-maintenance kept walking around on the one backend whose whole point is that nobody is on it. Measured: the change was noticed and logged within four seconds and the player had not moved three minutes later | **closed the same day, decided by the owner.** A changed flag now forces the same pass a phase change forces - `PlayerRouter#rerouteAll`, which was already public for this (its javadoc said *a future admin command can force one*) and which re-reads each player's own admission row rather than trusting the phase handed in. Inside the `changed > 0` guard, so an ordinary poll tick still costs nothing. Verified after the redeploy: *the admin flag changed for 1 connected player(s); re-routing* and *1 of 2 players moved* in the same second, and the SMP empty afterwards. `AdminChangeRoutesTest` pins the call, the guard it sits in, and the router contract it rests on |

| 142 | **New 2026-09-07, from the final CodeRabbit review of PR #8, and it is the only one of the five that duplicates items rather than losing them.** A grave is open to anybody - that is a decision, and it is written into `Graves`' own class comment - so two people can right-click the same grave in the same second. Each viewer got an inventory **of their own**, both filled from the same stored blob, and each close serialised its own whole snapshot back over the other's: both looters took the lot, and the grave paid out everything the dead player had been carrying, twice. Nothing in the game hints at it - both windows look exactly like a grave being emptied, which is the same reason findings 132 and 133 survived a real loot | **closed the same day** - one window per grave, shared by every viewer, which is what a vanilla chest does and needs no rule of its own; the write-back waits a tick and settles only once the last viewer has left, because Bukkit fires the close *before* it drops the viewer and "is anybody left?" has no honest answer inside the event. `OneGraveOneWindowTest` pins all three halves. **Not rehearsed in a world**: two clients on one corpse, and no client was running when the fix was written |
| 143 | **New 2026-09-07, from the same review.** A duel loser who is *dead* when the duel is settled cannot be written to - Minecraft replaces a dead player's contents on respawn - so `Duels` parks their pre-duel snapshot in a map and hands it back from `PlayerRespawnEvent` (finding 122). That map is one process's memory and nothing persists it, and the wait has **no time limit at all**: somebody who dies and walks away from the keyboard holds the snapshot for as long as they like. A restart in that window loses their own inventory for good and leaves them the arena's loadout - finding 122 exactly, reached by a different road | **closed the same day** - the button is pressed for them, which settles it on the next tick and leaves a window one tick wide. It is not a new decision either: the owner decided on 2026-09-06 that a duel ends at the spawn with a title and **no death screen**, and this branch - reached only by `/kill`, the void and `setHealth(0)` - was the last one still showing it. `DeadFighterIsRespawnedTest`. **Not rehearsed in a world** |
| 144 | **New 2026-09-07, from the same review.** `ensureRows` writes one row per milestone and one per objective, and `ensureObjective` **updates the target on conflict** because lowering one is the documented escape hatch. `ObjectiveEngine` reads that target from the database row rather than from the running track - so this is not the harmless insert of rows nothing looks at yet. Statement by statement, a failure in the middle (a connection that stopped answering inside `query-timeout-seconds` is the ordinary way) left some objectives carrying a candidate file's targets and the rest the running track's, with `track` unchanged and the log line saying the reload had been **refused** | **closed the same day** - one `jdbi.useTransaction`, so the reported outcome and the database agree. `TrackProvisioningIsAtomicTest` pins the transaction and the ordering finding 103 established; exercised on the local stack, where enable and a `/smp reload` both still reach *the milestone track was reloaded: 8 milestones* |
| 145 | **New 2026-09-07, from the same review, and it is the fourth time this repository has been told a world spawn is a coordinate rather than a promise.** `LandingSite#safeAt` ends with the preferred spot when its search comes back empty. That fallback is right for a duel that has to end and for a farm world that is about to be deleted - not arriving is worse than arriving badly - and wrong for the balloon, whose success path finishes by telling the traveller they arrived. Rare is not the same as unreachable: findings 124 and 134 were both *rare* | **closed the same day** - `findSafeAt` hands the emptiness back, the balloon reuses the "destination unavailable" branch it already had, and `safeAt` is unchanged for the two callers that must not refuse. `LandingIsCheckedTest` accepts either helper and says which is for whom |
| 146 | **New 2026-09-07, from the same review, and it is a finding about a test.** `BundleContinuationTest`'s parenthetical-plural check read **raw lines**, so a comment in a bundle explaining the rule - `# never write spin(s)` - would have failed the rule it explains, and a key could have tripped it too | **closed the same day** - the check parses the value out of each line and skips comments; verified by putting both cases into a real bundle, the comment green and the value red |
| 147 | **New 2026-09-07, seen while deploying the four fixes above.** `deploy/dev deploy smp` installs the new jar and then restarts the container - which is the shape production has too, because the updater stages jars into `plugins/` while the servers are running and the restart follows. The running JVM's jar is therefore replaced underneath it, and every class it has **not yet loaded** is gone: `onDisable` died with `NoClassDefFoundError: eu.nordtal.s2.smp.duel.Duels$ActiveDuel`, because no duel had run in that container's life. The step isolation added on 2026-09-06 caught it (*disable step duels.stop failed; carrying on with the next one*), so the rest of the shutdown ran - what was lost is the arena teardown and, if a duel had been in progress, both fighters' saved inventories. The rule "the running process carries on with the old jar" holds only for classes already loaded, and nothing anywhere says so | **closed 2026-09-07/08, and the fix was a redesign because the alternatives were both worse.** Decided by the owner after seven grilling rounds: **an update is one run** - 30 s countdown, stop the services whose jars change, migrate, swap, start, and poll Arcane's `/runtime` until each one reports `running` **and** `healthy`. `APPLY` is deleted rather than repaired (a button that swaps a jar under a live server is the defect with a label on it), the host's `apply` became `bootstrap` and installs only into *empty* slots, and `UpdateKind.APPLY` survives solely so the rows already in the table still map. What made the redesign possible was reading Arcane's source rather than its docs: its `restart`/`update-services` calls are service-selective and its container-level `stop`/`start` are separable - so the updater can leave a gap for the swap **and never stop itself**, which is why it is now alive afterwards to say whether anything came back. Two things fell out of it that nothing had noticed: the old `redeploy` sends no body, so `forceRecreate` is false and compose recreates only *diverged* containers - our jars live in a bind mount, so **the old restart button very likely restarted nothing at all** (which is arcane#1943's symptom from our side); and `pendingRestart()` filtered `kind = 'RESTART'`, so an update would have taken servers away with **no countdown shown anywhere**. `UpdateRunTest` (10) holds the sequence against a fake Arcane, `UpdateReportsTest` (10) the report that replaced the code block. **Nothing has run**: no 2xx has ever been seen from this Arcane, and the run refuses to start without one - see [`todo.md`](../../todo.md) |
| 148 | **New 2026-09-08, from the owner's review of every open item.** Velocity's `/server` is open to **every player** by default - the command checks its permission for an explicit `FALSE` only - and `network-control` handles neither `PermissionsSetupEvent`, `CommandExecuteEvent`, `PlayerAvailableCommandsEvent` nor `ServerPreConnectEvent`. A player in the SMP phase can type `/server hunger-games` and land there, past the routing. On Paper, players see and can run `/me`, `/help`, `/trigger`, `/list`, `/tell` and the whole vanilla tab completion | **open, agent work.** Decided by the owner: `network.yml` carries an **allowlist** of player commands, a filter on both platforms enforces it and limits suggestions to it, admins exempt. `aufgaben.md` B3 |
| 149 | **New 2026-09-08.** `hunger-games` has **no chat renderer and no system lines**: chat, join, leave, death and advancement are vanilla there - yellow lines, no flag, no crest - while `smp` has `SystemLines` and `PresenceListener#onChat`. One network, two looks | **open, agent work.** Lift both into `:paper-common`, use them on `hunger-games` (with a kill feed in the same style), and inventory every remaining vanilla text a player can see - unknown command (`UnknownCommandEvent`), sleep, kick texts, `/list` - to replace what can be replaced. `aufgaben.md` B5 |
| 150 | **New 2026-09-08, reported by the owner.** The spawn NPC (`Mannequin`, a `LivingEntity`) **can be killed by hitting it**. Spawn protection covers blocks, not the entity | **open, agent work.** Invulnerable plus a damage-event cancel, silent, no knockback; the enable sweep has to apply the flags to the persistent figure already on disk (finding 100). `aufgaben.md` B8 |
| 151 | **New 2026-09-08, and it is the one that can cost the deployed configs.** Arcane's GitOps sync **deletes ignored files** from the checkout (owner, 2026-09-08). `compose.yml` mounts `plugins/` as `${SERVERS_ROOT:-./deploy/servers}/...` since 2026-09-05 and production has the variable unset - so `config.yml`, `milestones.yml`, `sounds.yml` and `pack.yml` sit inside the synced directory. The `SERVERS_ROOT` comment in `compose.yml` named exactly this risk and left it as a variable to set | **open, agent work, first in line.** The default must be a named volume per service and the bind mount the exception the local stack opts into; `TopologyTest` asserts the default contains no path. The owner moves the four directories back into volumes once it is deployed. `aufgaben.md` B1, `todo.md` A1 |
| 152 | **New 2026-09-08, a decision rather than a defect.** `bot.yml#bunq.environment` offers `SANDBOX`, `bunq-accounts.py --sandbox` exists and three documents describe a sandbox run - and the owner has no sandbox key and decided a real 3 EUR purchase with a cancel-and-restart replaces the run | **open, agent work.** Retire `SANDBOX` (jcore drops the retired key with a `WARN`; `ConfigsTest` pins it as gone), the script flag and the three documents. `aufgaben.md` B16 |
| 153 | **New 2026-09-08, found by folding `/update` onto the proxy.** Velocity executes every command it knows **itself**, so the proxy's own `/update` was answered by a no-op watcher: the player who typed it got the acknowledgement and then nothing at all, for the whole run. The Paper watcher worked, which is why nobody saw it - the surface it was broken on is the one somebody uses *because* the network is misbehaving | **closed 2026-09-08** (`9fba30d`) - `UpdateFollower` in `:commands` decides finished/gone/timeout/report as a pure function, `UpdateWatch` drives it on the proxy scheduler every 2 s, and `UpdateWatcher` in `:paper-common` is now only the console's |
| 154 | **New 2026-09-08, the same day, and it is the second time this repository has learned it.** `/update`'s root node carried no `requires`, on the reasoning that its children are gated - and both Brigadier adapters reached the root default by calling the child's dispatch **directly**, which goes around exactly that check. Any player on either platform could read the whole update report. Finding 102 is the same defect on `/phase`, closed two days earlier, and the comment left behind there said the child applies the check | **closed 2026-09-08** (`9fba30d`) - the root is gated where a subtree is entirely admin-only, plus an admin check inside `run()` of both adapters; `PaperCommandsRootGateTest` and `VelocityCommandsRootGateTest` |
| 155 | **New 2026-09-08.** Every `/update` a console typed was written with the asker's own id as `requested_by`, including the proxy's console, which has none - so the column said a person had asked for runs nobody asked for | **closed 2026-09-08** (`9fba30d`) - the source comes from `origin()`: console writes `null`, Discord writes the id (`DirectoryUpdateEffectsTest`) |
| 156 | **New 2026-09-08.** Discord cannot execute a root that also has subcommands - it becomes a menu - so `/update` alone, the read-only report, was **unreachable in Discord**: the one surface where somebody checks before deciding | **closed 2026-09-08** (`fe3d14b`) - the report is `/update check` there, `Catalogue.ROOT_DEFAULTS` keeps bare `/update` working in game, and `DiscordCommandsTest#noRootIsBothACommandAndAMenu` fails the next command that would do it |
| 157 | **New 2026-09-08, and it is why the countdown was worth rebuilding.** The countdown was written **before** the plan was resolved, so a run with nothing to do still counted every player down thirty seconds and then did nothing; and the proxy drew it from a 5-second poll, so the number a player saw was up to five seconds behind the moment the servers actually went away | **closed 2026-09-09** (`eeaf03f`) - `Runner` resolves first and only then writes `COUNTDOWN` (no work, no countdown), `startCountdown`/`commitCountdown` make the cancel window atomic, and the proxy schedules a beat on the exact millisecond instead of polling for it; `CountdownTest` (13) against a `MutableClock`, `CountdownComesAfterResolvingTest` (3) against `Runner`'s own source |
| 158 | **New 2026-09-08.** `pendingRestart()` filtered `kind = 'RESTART'`, so an **update** - which also takes every server away - showed no countdown to anybody | **closed 2026-09-09** (`eeaf03f`) - `countingDown()` matches both kinds and both in-flight statuses; `V13__countdown_after_resolving.sql` widens the partial index to match |
| 159 | **New 2026-09-08.** `settleOrphans` inferred that a restart which had outlived the updater had **succeeded** - a rule written when a restart stopped the updater itself, which stopped being true on 2026-09-07. It therefore reported success for runs whose outcome nobody knows | **closed 2026-09-09** (`eeaf03f`) - `completeOrphanedRestarts` is deleted and every `RUNNING` row is failed at boot, which is what the process can actually say |
| 160 | **New 2026-09-09, reported by the owner from the running network.** The SMP's MOTD showed **nordtal.eu in green**. Each of the five phase MOTDs coloured the brand name for itself - a light blue gradient before the opening, orange in the event, green on the SMP, grey in maintenance - so the server browser advertised four different marks depending on the day | **closed the same day** (`897963f`) - one literal, `NetworkSpec.MotdSpec#NORDTAL_BLUE`, measured off `resource-pack/src/pack.png` (`#24357d`, lightened to `#4a63d8` for the near-black list); `BrandColourTest` fails a phase that colours the name again, and `docs/presentation.md` carries the rule |

## 2. What can be built today

Everything in §2 of the previous version — the phase model, the admin flag, the language list and
the join-time locale component, the two `network-control` config decisions, the glyph clean-up and
the play time counter — **is built**. What follows is what is genuinely left, in dependency order.

### a. `limbo`, and the pack station in `network-control` — **built 2026-09-01**

The three artefacts no document named have been decided and written:

- **The channel is `nordtal:limbo`**, and it runs both ways: `READY` from the backend once per join,
  `WAIT <reason>` from the proxy whenever the reason changes. Two bytes of header, one UTF string for
  a `WAIT`, and the codec in `:common` so the two ends cannot drift. **Once per join is exactly what
  made it dangerous** — see finding 38: the release now survives that `READY` arriving early, late or
  not at all.
- **The waiting reasons are `PACK`, `BACKEND`, `MAINTENANCE` — and `UNKNOWN`**, which is the fourth
  nobody asked for and which exists because a black screen with no text on it is what a crash looks
  like. All four are translated in both languages.
- **The pack URL and hash live in `pack.yml`**, a new file in `network-control` with its own
  environment namespace. Not `gate.yml`: these change on every pack release and that file decides
  who may join.

**What is left is the rehearsal, and it cannot be done from a build machine.** The three open
verifications this session owned are still open, each is now a numbered step in the owner's
checklist, each is a row in [the table below](#the-unverified-assumptions) with its written fallback —
so the phase is *built and unproven*, which is a different thing from *unbuilt*.

One measurement did come out of it. `curl` against a real GitHub release asset on 2026-09-01: one
`302`, to **`release-assets.githubusercontent.com`**, with a signed URL that expires within the
hour. This knowledge base said `objects.githubusercontent.com`, which is wrong and is corrected. It
does not settle whether a Minecraft client follows the redirect — only where it goes.

### b. The SMP's server-free core — **built 2026-09-01**

**Specified by** [smp.md](smp.md). **Touches** `smp`. **Depends on** V6, which exists.

Three pieces of the SMP are pure logic, testable the way `BorderMath` and `Demotion` are tested in
`hunger-games`, and none of them needs a world, a packet or a player:

- **Aura.** The ledger, the death penalties, the duel stake, and the payout formula — 30 % equally
  among qualifiers, 70 % by share, a 2 % qualifying threshold, a minimum of 1 aura per qualifier
  taken out of the proportional part, everything floored. The rounding cases are exactly the kind of
  arithmetic that is cheap to test and expensive to discover in production.
- **Prestige.** The tier derived from `player_playtime.seconds` against the thirteen configured
  thresholds. Never stored, so it is a pure function.
- **The milestone engine.** The YAML loader and its validation — which must refuse a change that
  orphans stored progress while explicitly permitting a lowered `target`, or the first escape hatch
  does not exist — progress accounting per objective type, and the three escape hatches with their
  `pot × (reached ÷ target)` payout.

### c. The SMP's world half — **built 2026-09-01, unverified**

Every feature here ends at a rehearsal rather than a green build. The work was cut into three
blocks on 2026-09-01 so that each ends somewhere a person can look at it:

| block | contents | state |
|---|---|---|
| 1 — the world | bootstrap, datapacks, borders, balloon and its GUI, portal gating, the farm-world swap, spawn protection | **built** |
| 2 — the surfaces | HUD, the per-player Text Display boards, nametags, chat, `/navigate`, POIs, prestige rendering | **built** |
| 3 — the activities | duels, graves, the wheel, milestone completion and its payout, `/smp` | **built** |

All three landed on 2026-09-01. The rehearsal that has to follow them is a checklist for the owner,
not a document, and lives in the `todo.md` outside this repository.

**Measure Nordtal's one-off pre-generation to border 4000 in this session's first hour**, not its
last. It is the cheapest measurement in the plan and it is the one that decides whether the final
milestone is deliverable at all.

**Nametags come from `papermc-display-tags`** — decided 2026-09-01. `smp` takes
`com.github.nordtal:papermc-display-tags:2.0.0` from JitPack as `compileOnly` and declares
DisplayTags in its `paper-plugin.yml`. The consequence is operational and is stated in
[../deploy/README.md](../deploy/README.md#third-party-plugins): **PacketEvents becomes a required plugin on the
SMP server**, which made it the network's first mandatory third-party runtime dependency.

**Chunky became the second on 2026-09-01**, and unlike DisplayTags it is required for a mechanic
rather than for a rendering: the farm world is pre-generated every night, roughly 15 000 chunks
beside a live server, and the reset waits for Chunky's completion event before swapping anything in.
`org.popcraft:chunky-common:1.5.3` from CodeMC, `compileOnly`, driven through Bukkit's
`ServicesManager`. CoreProtect remains the only third-party plugin that may be absent without
anything failing.

**Terralith and Dungeons and Taverns are world-generation datapacks the whole design depends on and
that no document mentioned before 2026-09-01.** They are pinned by sha512 and fetched by the
container entrypoint into the `level-name` world's `datapacks/` folder before the server starts;
`smp` verifies they are enabled and refuses to start otherwise. Measured the same day: datapacks are
server-global, read only from that one folder, with no per-world API — so the nightly farm world
inherits them with nothing copied.

### d. The hunger games winner's head start

**Specified by** [hunger-games.md](hunger-games.md#after-the-game) and
[smp.md](smp.md#the-hunger-games-winners-head-start). **Touches** `smp` only.

**Built 2026-09-07** — `eu.nordtal.s2.smp.headstart.HeadStart`, `SmpDao#startEventWinner` and
`#grantHeadStart`, eight integration cases against a real PostgreSQL and two wiring cases. With it,
**§2 is empty**: there is nothing left in this document that can be built without a server.

Decided 2026-09-01, and the decision moved the work: **the SMP grants it on the winner's first
join**, deriving the winner from `hg_game.winner_member_id` of the `DECIDED` game.
`hunger-games` writes nothing into the SMP's tables. V6's
`smp_player.hg_winner_reward_granted` is the only state involved, because it is the only thing the
SMP cannot derive.

## 3. What still needs a decision

No design decision blocks an implementation session. What is listed here is either concept work of
its own, or a config default that is cheaper to propose in a diff than to argue in prose.

### Concept work — the one item here is now built

**PostgreSQL backup and restore — built 2026-09-01, and the cycle was run.** `deploy/`'s new
`backup` profile dumps the database into a `postgres-dumps` volume of its own, daily and at
start-up, and Arcane's volume backup ships *that* volume to S3. The shape was decided by reading
Arcane's source rather than its release notes: it stops the containers using a volume only when the
policy's `StopContainers` flag is set, and for a live PGDATA both settings are wrong — off gives a
torn copy that fails at restore rather than at backup, on gives a nightly outage of every process in
the stack. Dump → `pg_restore` into an empty database → both seeded tables back, plus a retention
sweep and a SIGTERM, all run on 2026-09-01; see
[../deploy/README.md](../deploy/README.md#what-was-measured). The first run on a fresh named volume
*failed*, on ownership, which is the argument for running it.

**What is still owed is the drill on the real thing**: a dump pulled back out of S3 and restored.
That needs the host and is in the owner's checklist. Backups that have never been restored are a
belief, not a backup — which is what this row said when it was a gap, and it still applies.

**The deployment runbook is no longer part of that gap at all.** SimpleCloud was dropped on
2026-09-01 in favour of a single `docker compose` stack, and the stack was built the same day:
[`deploy/`](../deploy) holds the compose file, the image for all four Minecraft services, the
entrypoint and the runbook. It is **measured, not just written** — a Velocity and a Paper container
were run from that image, the pinned build resolved and checksum-verified through the Fill API, the
console proved writable from a `docker exec`, and `docker stop` shut Paper down in 3 s with
`All dimensions are saved` in the log. See
[../deploy/README.md](../deploy/README.md#what-was-measured-and-what-it-cost), which also records the one design that had to be
thrown away.

What is left there is one open verification and one manual step, not a build: whether the
*interactive* `console` attach behaves inside Arcane's browser terminal (`mc <command>` through a
plain exec is already proven, and is the fallback), and pasting the Velocity forwarding secret into
each backend's `config/paper-global.yml`. Neither blocks anything before the first deployment.

### Config defaults — **there are none left open**

This section used to carry a table of six. **All six were proposed as defaults on 2026-09-01**, in
the same session that built the SMP's server-free half, and they are in the code: `DefaultSmp`
holds the advancement awards, the "embarrassing" death causes, both duel loadouts, the winner's
items, the wheel's prizes and weights and the spawn regions, and `DefaultTrack` holds the items and
advancements behind every objective of the track. [smp.md](smp.md#still-open) says the same thing.
The table survived that session because it lives in a different document from the code it described
— see finding 14.

They are *proposals*, and the whole point of putting a proposal in a config file is that correcting
it is a diff rather than an argument. [The numbers that are proposals rather than
decisions](smp.md#numbers-that-are-proposals-not-decisions) is the list to read before retuning any
of them.

The hunger games' own three — loot pools, quiet period, passive shrink rate — were proposed as
config defaults on 2026-08-31 and are likewise no longer open.

### Technical choices an SMP session has to make for itself

None of these is a design decision the concept withheld; they are implementation choices the concept
never had reason to name, and an implementation session should make them and write them down rather
than stop.

**Made and written down on 2026-09-01:** the spawn NPC is a `Mannequin`, a vanilla Paper 26.2
entity with a real player skin and no AI — none of the three options this list weighed was needed;
the milestone file format is `milestones.yml` behind `MilestonesSpec`; `smp_grave.contents` is
`ItemStack.serializeItemsAsBytes`; the spawn regions are `config.yml#spawn-regions`, and the same
box shape carries the balloons; the balloon GUI is a 2 × 2 grid whose wide upper entry is always
the overworld you are not in.

- The contents of the **hand-in and wheel GUIs** beyond what the concept states.
- **How a duel arena's schematic is loaded**, when one exists. The first version is a glass box the
  plugin places and removes itself, so this is not blocking anything.

### Not decisions at all — writing, drawing and building

| open point | source |
|---|---|
| The server rules as written for players, both languages — before the SMP phase opens | [smp.md](smp.md#still-open) |
| The spawn build: tavern, balloon model, duel platforms, boards, NPC — with the balloon between radius 10 and 21.5 | [smp.md](smp.md#spawns) |
| The hunger games world folder, lobby, towers, loot points, and the aerial images per language | [hunger-games.md](hunger-games.md#the-lobby) |
| The real artwork behind the generated placeholder glyphs | [resource-pack/README.md](../resource-pack/README.md#code-point-allocation) |

### The unverified assumptions

Nothing below has been confirmed, and **every one of them has a written fallback** — which is the
point: an unverified assumption with no fallback is a decision nobody has made yet, and one with a
fallback can be answered *no* without blocking anything. The fallbacks are design and live here.
**The steps that produce the answers do not**: they need a running server, a real client or the
production host, so they are a checklist for the owner rather than a document, and they live in a
`todo.md` outside this repository. Ask for it; it is deliberately not published.

| what is unverified | who owns the answer | **if the answer is no** |
|---|---|---|
| ~~**A Minecraft client follows GitHub's redirect** when downloading the pack. The redirect itself is measured ([resource-pack/README.md](../resource-pack/README.md#hosting)); whether a *client* follows it is not~~ | **answered 2026-09-07, yes** - against the real `v0.5.1` release URL: one redirect, the client downloads and applies, the glyphs draw. The small static host is not needed. Learned on the way: the client caches packs **by hash**, so a broken URL is invisible to anybody who already holds that `sha1` |
| ~~**A forced pack offer sent by the proxy while the player is being moved to `limbo`** behaves, and both refusal paths work — including whether `PackStation`'s own `disconnect()` wins the race against Velocity's generic forced-pack kick, which is the only way our `DECLINED` text reaches a 1.17+ client~~ | **answered 2026-09-05/07** - the offer arrives in `limbo`, applies, and releases. On a forced offer the *client* disconnects itself on Decline with its own text, so neither `pack.declined` nor Velocity's line is ever seen (finding 94); `FAILED_DOWNLOAD` reaches `pack.failed-download` verbatim (finding 140) |
| ~~**Disconnecting a player from `PlayerChooseInitialServerEvent`.** Since 2026-09-01 a proxy with no waiting room refuses *every* login rather than letting anybody past the pack station. `player.disconnect()` is documented and the event is `@AwaitingEvent`, but a login-allowed player being kicked *during* initial server selection has not been seen happen~~ | **answered 2026-09-07, yes** - `server-limbo` pointed at a server that does not exist: the player sees `gate.no-server`, our text wins, no silent landing on Velocity's `try` list. Velocity's own *no available servers* line lands in the log only |
| **Background pre-generation of a 2000 × 2000 world without perceptible lag.** The concept's own biggest technical risk | the SMP session, measured on the real host with players online | The farm world gets smaller — 2000 × 2000 is [a proposal](smp.md#numbers-that-are-proposals-not-decisions) and halving the radius quarters the work. If even a small world lags, pre-generate off-peak only or in a separate process and move the folder in. Operational, never a redesign |
| **Nordtal's one-off pre-generation to border 4000** is affordable in wall clock and disk | the SMP session, on the real host | The final border is the number to reconsider. It is a config default in `milestones.yml`, and [the track](smp.md#the-track) is explicit that every number in it is a default while the *rules* are the decision — so a smaller frontier is a retune |
| ~~**Paper unloading and deleting a loaded world at runtime**, then loading a replacement under the same name~~ | **answered 2026-09-01** | **Confirmed green**, headless, on Paper 26.2 build 121: three consecutive rounds, 27 checks, `unloadWorld` releasing the folder, the folder deleted, another renamed into its place and the same name re-loaded carrying the replacement's seed, no stale `session.lock`. The two-name fallback is not needed and was not built. Two things the drill also produced: the swap window of 15–18 ms was on tiny flat worlds, so the real reset renames the old folder aside and deletes it off-thread; and a Bukkit-created world lands at `<level-name>/dimensions/minecraft/<name>`, not beside the primary world |
| ~~**`LISTEN`/`NOTIFY` through the pool**~~ — a dedicated connection outside Hikari, a `getNotifications(timeout)` thread, an unconditional re-read on every reconnect | **half answered 2026-09-01** | **That a notification crosses a container boundary is confirmed**, measured on the `updater`'s `nordtal_update` channel between two containers on a Docker network: a request written with `pg_notify` was claimed in **160 ms**, and the identical request written without one took **17 s** — the poll. **The reconnect half was answered 2026-09-06**: all five listener connections killed with `pg_terminate_backend`, every one logged the drop and was listening again after 5 s, and an admin revocation right after arrived in ~4 s over the new connection |
| **Arcane's redeploy endpoint** — narrowed 2026-09-01 and again 2026-09-05, still not closed. The path is known (read from Arcane's source); the environment and project **IDs** and the token are on the production host, and arcane#1943 says a 2xx would not by itself prove the restart happened. **What 2026-09-05 added is that the first real attempt never reached Arcane at all**: `ARCANE_URL` was `http://docker.host.internal:3553`, Docker's three labels in the wrong order, and the whole of what reached the admin channel was `java.net.ConnectException` — the JDK wraps a DNS failure in an exception it gives no message to, so a bad name, a refused connection and a dead route all print the same eleven characters. The message now carries the cause chain and names both wrong values by hand (`Arcane#causeChain`, `#loopback`, `#transposedDockerHost`); correcting the value is the owner's, in [`todo.md`](../../todo.md) | the first deployment; [`todo.md`](../../todo.md) | Leave `arcane.base-url` empty. Everything else in the updater works, both surfaces say "Arcane is not configured", and the redeploy is a click in Arcane. **A 2xx from a real Arcane has never been seen**; the 404, 401, 503 and unreachable branches are all tested, and a 404 was driven end to end through the container. Since 2026-09-05 the path can also be rehearsed against an Arcane on a development machine — see [deploy/README.md#locally](../deploy/README.md#locally) |
| **`console` — the *interactive* attach — behaves inside Arcane's browser terminal.** `mc <command>` through a plain `docker exec` is [already verified](../deploy/README.md#what-was-measured-and-what-it-cost) | the first deployment | Use `mc` plus the log view — send-and-read without a TTY, which covers every command a runbook issues. Only live scrollback is lost, and `docker exec -it <container> console` over SSH still gives it |
| ~~**Operator actually covers what an admin needs**, including nodes belonging to third-party plugins nobody enumerated. Replaced `smp`'s six-node `PermissionAttachment` on 2026-09-04, and no test here can say anything about it: `AdminOperatorsTest` proves the *policy*, not that Paper's operator flag opens the doors we think it does~~ | **answered 2026-09-06/07 on `smp` and `hunger-games`** - `/difficulty`, a command in none of the six retired nodes, answers for the admin and is *Unknown or incomplete command* for a non-admin; the live revocation removes it within seconds on both. `limbo` is proven by its log line only, because since finding 141 a revoked admin is moved out of the waiting room in the same moment |
| ~~**`ops.json` is empty after a restart on every Paper server.** The enable-time sweep is what makes an operator a property of the session rather than of the disk, and it is the half of this change that a crash can defeat~~ | **answered 2026-09-07 on `smp`** - a hand-`op`ed second entry and the admin both in the file, `docker compose restart smp`, `ops.json` is `[]` right after `Done`, and only the database admin comes back on reconnect. The same file on `limbo` and `hunger-games` is unseen but runs the identical code |
| ~~**A second login in one client session leaves the waiting room.**~~ | **answered 2026-09-05, on the local stack with a real client** | It did not, and the reason was neither of the two guesses this row carried (a session outliving the disconnect, or the sweep not running): an admin's `STAY` at login was `velocity.toml`'s `try` list, which is the waiting room, and the release then pointed back at it. Finding 93. Three consecutive local joins now land on `smp` within eight seconds; the production check after 0.6.0 is in [`todo.md`](../../todo.md) |
| ~~**Simple Voice Chat on 26.2**~~ | **answered 2026-09-08: a build exists** - `bukkit-2.6.23`, Modrinth, tagged 26.2 (2026-09-04). The owner chose to install it, optional for players with the mod; it becomes an updater artefact (`aufgaben.md` B14), and the Velocity half of it is the research question there |
| **Proxy-only pack enforcement**, which would make `limbo` unnecessary for packs | after the event, never on the critical path | Nothing changes — `limbo` stays, which is the current design. The one row that can only *save* work, which is why it is last |

**When one of these is answered, move the row out of this table** and write what was actually
observed, with the date. A *no* is a result, not a failure.

## 4. Recommendation

One implementation session is left, plus two rehearsals. The concept session that used to be
here was the backup concept, and it was built on 2026-09-01.

- ~~**`limbo` and the pack station** (§2a)~~ — **built 2026-09-01.** What it left behind is a
  thirteen-step rehearsal against a running proxy and a real client, which is an afternoon with a
  Minecraft account and not a session. Three rows of the table above fall out of it.
- ~~**The backup concept** (§3)~~ — **built 2026-09-01.** What it left behind is one restore drill
  against S3 and the host, which is an afternoon and not a session.
- ~~**The SMP's world half** (§2c) plus the winner's head start (§2d)~~ — **both built**, the world
  half on 2026-09-01 and the head start on 2026-09-07. **There is no implementation session left in
  this document**: §2 is empty, and what every one of those features left behind is a rehearsal
  rather than a build.
- **The pre-generation measurement**, which does not need this repository at all and should happen
  before the world half is designed around a number nobody has. It is two numbers — Nordtal's
  one-off generation to border 4000, and what the farm world's daily generation does to the 95th
  percentile of tick time with players online — and the recipe is in the owner's checklist.

One thing to keep in view throughout, **rewritten 2026-09-06 because it had become false**: it used
to read "nothing here has been exercised against a running server, a real client, Discord or bunq".
The first two halves stopped being true on 2026-09-05, when the whole network came up on the local
stack and two real clients walked through it - and the twenty findings from those two days are what
that paragraph was worth. **Discord and bunq are still untouched**, and so is the production host,
and so is every world: `smp` expects Nordtal and `hunger-games` expects its arena, neither of which
is in this repository. The test suite covers the access API, the config specs, the fallback cache,
the tier arithmetic, the border and colour maths and the phase directory against a real PostgreSQL
container. It touches no packet, no player and no bank. Every session above ends with a rehearsal,
not with a green build.
