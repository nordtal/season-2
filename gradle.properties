# Single source of truth for the version of every season 2 artifact; a release tags `v<this>`.
version=0.9.5
group=eu.nordtal

org.gradle.caching=true
org.gradle.parallel=true
org.gradle.configuration-cache=true

conventions.root.comments=true
